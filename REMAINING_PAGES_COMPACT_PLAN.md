# 剩余用户页面：精简、一致与手机适配执行计划

日期：2026-09-10。状态：仅规划，未执行。

## 1. 范围与执行依据

依据当前 app.json 的 35 个路由和剩余页面 WXML 梳理，共覆盖 20 页。首页、我的、约爬详情、发布页已由其他 agent 执行，本计划不修改这四页；共享组件变更必须与该 agent 协调。

普通用户 16 页：calendar-mine、profile-edit、friend-list、circle-list、circle-detail、circle-edit、card-view、card-gift、card-claim、wall、calendar-overview、calendar-timeline、checkin、activity-records、notifications、safety。

馆长业务 4 页：owner、gym-manage、gym-merge、gym-merge-result。

debug-logs、review-queue、user-admin 和全部 backstage 系列共 11 个页面仍不设计；safety 内管理员举报区只保持可用，不扩展后台设计。

本文件覆盖原 FRONTEND_REDESIGN_EXECUTION_PLAN.md 对上述 20 页的构图建议。首页/我的仍以 FRONTEND_SAMPLE_REFINEMENT_PLAN.md 为准，详情/发布以 DETAIL_PUBLISH_REFINEMENT_PLAN.md 为准。保留已有深色主题，不再增加配色方案。

本次为代码结构核对和设计交接，未取得这 20 页的最新真机截图。下文的尺寸、密度和交互为待实现验收要求，不代表当前已满足。

## 2. 四条全局规则

### 2.1 一个功能一个主要位置，一个内容块不重复表达

- 同屏相同标题、人数、说明、主操作只保留一处。原生导航已显示页名，正文不再重复同一标题。
- 跨页不复制整套相同业务工具：时间轴不再维护另一套报名/移除名单；日历不再附一套完整岩友圈列表。
- 对象必要上下文可以保留：活动列表需要馆名，活动详情也需要馆名；这不属于应删除的重复工具。列表给摘要，详情给完整内容，不能为了“只出现一次”让详情失去辨识信息。
- 状态转换前后复用同一个操作槽，例如“生成领取码”成功后变“转发”，不在下方再新增第二组按钮。
- 删除入口前必须确认唯一目的地可达。若唯一入口在别的 agent 所负责的页面，先记录依赖并协调，不能本包先删到功能失联。

### 2.2 用标签、选中态和操作结果说明用途

- 删除“嗨，某某”“下面是你的……”“点这里……”“当前草稿ID”等开场白和技术说明。
- 字段保留明确标签，placeholder 只放示例；图标不能替代所有操作文字。新增、保存、接受、查看等保持清楚名称。
- 保留做决定必需的信息：付出次数、数据迁移影响、公开范围、权限、失败原因和校验错误。它们就近出现，不写成大段全页说明。
- 空态一行事实＋一个去向，例如“还没有岩友 / 去找搭子”。加载中、失败、无权限不能当空态。
- 无数据不显示占位破折号群、虚假数量、默认热度或假头像堆；未知与零分别处理。

### 2.3 同一种东西使用同一套组件

以四页最新通过的 compact 样板为唯一视觉基线。共享实现由一个负责人维护，不让各页重新画筛选、箭头和标签。

| 元素 | 全页统一规格 |
| --- | --- |
| 导航与正文标题 | 页名交原生导航；正文对象名 36rpx/600，区块标题 30–32rpx/600；本包不使用 54rpx 大口号 |
| 正文与辅助 | 正文/按钮/输入 28rpx，辅助 24rpx；常规字重 400/500/600；状态不能靠 20rpx 小字表达 |
| 城市入口 | 无边框文字＋统一线性下箭头、垂直居中、88rpx 热区；沿用首页最终版本 |
| 筛选框 | 紧凑弱底色、统一圆角与内边距；值弹性收缩、箭头不收缩；默认短值必须完整 |
| 选择弹层 | 城市/岩馆长列表统一 sheet：标题/关闭、必要搜索、选中标志、结果、加载/错/空；多选有完成，单选点选即应用 |
| 日期/时间 | 原生 picker＋统一触发器；时间轴/日历为专用视图，不能为了统一将其改成普通 picker |
| 分段导航 | 同一 compact segmentedTabs；当前项弱绿底，至多四项；同一页不叠两组同义导航 |
| 标签 | 同样的文本标签与颜色语义；可选标签有勾选/选中态，纯状态标签不画成按钮；圈色不改变通用状态色 |
| 人物行 | 64–72rpx 头像＋名字＋一行辅助，右侧状态或操作；min-width:0，长文自然撑高 |
| 普通功能行 | 88–96rpx 起步；箭头统一资产，右侧值不挤压关键名称；不加无意义第二行解释 |
| 按钮 | 一个主要动作；固定按钮 96rpx 高、宽度100%；次操作中性文字，危险操作独立；显式 width/margin，验证微信默认样式 |
| 间距 | 页面左右32rpx，320px宽可24rpx；组间24rpx、组内12–16rpx；一组不用标题/描边/底色三重分隔 |

色彩继续 user-theme.wxss 的 canvas/ink/accent/success/pending/danger。禁止同类选择器有的金色有的紫色，或每页不同箭头字符。图标用统一资产，不新增 Emoji 控件。

### 2.4 手机屏幕：内容完整，不硬撑一屏

- 短内容页使用可用视窗的 min-height 和弹性留白，不设 overflow:hidden 硬裁；字号放大或内容变长后自然转为可滚动。
- 长表单/列表默认只有页面本身一个纵向滚动容器；不在页面中间塞固定高度的小滚动名单。弹层、时间轴属于明确例外。
- 不用 100vh 减去猜测的导航/Tab 像素；用小程序实际视窗与布局验证。原生导航、原生 Tab、底部安全区只计算一次。
- 有固定操作栏时，内容底部占位与实际栏高一致。最后一行、分页按钮和错误提示必须能完整滚到栏上方；无固定栏不留统一 200rpx 空白。
- 普通行高不固定，不能用固定 height 加隐藏溢出裁两行馆名。日期、状态、金额/次数、确认动作、错误信息不得省略。
- 标题或简介需要省略时有明确查看全文位置；不能将名称/关键识别信息永久截断。横向双列放不下时改纵向，不持续缩字。
- 滚动中屏幕边缘经过半张卡是正常行为，无法保证每个任意位置都以整卡结束。要求是内容没有内部裁切，停留在顶部/底部可读，能顺畅滚到完整内容；不启用强制吸附翻页凑整屏。
- 固定头部仅保留必要筛选，不能多层 sticky 吃掉大半屏。弹层最大高度以实际视窗约80%为起点，内容区可滚，头尾固定；键盘弹起后输入和操作可见。

## 3. 入口归属表

| 功能 | 唯一主要入口/完整操作页 | 本包需去重 |
| --- | --- | --- |
| 约爬通知 | 我的 → notifications | 删除 calendar-mine 的通知入口；不另加铃铛 |
| 攀爬记录 | 我的 → activity-records | 删除 calendar-mine 的记录入口 |
| 发起约爬 | 首页固定按钮 → calendar-publish | 日历/时间轴不再各摆发布大按钮；空态回找搭子即可 |
| 活动报名/成员管理 | plan-detail | timeline 只选活动并打开完整详情，不复制写操作面板 |
| 岩友圈管理 | 我的 → circle-list → circle-detail | 日历移除圈列表、建圈按钮；圈筛选仍保留以完成日历过滤 |
| 名片完整预览与保存 | card-view | 列表/我的用预览入口；领取/赠送结果不重复铺导出工具栏 |
| 新建打卡 | activity-records 的“记录攀爬” → checkin | 日历移除打卡按钮前先完成此入口并核验无馆时选择流程 |
| 馆长工具 | owner → gym-manage | 合并只在管理页危险操作区进入；结果页不再诱导连续合并 |

深链接、原生返回及操作完成后返回属于必要导航，不能删除。发送名片仍有 card-gift 自身的赠送主动作；名片查看的保存与赠送的发送不是重复操作。

## 4. 逐页方案

### P01 calendar-mine｜我的约爬

顺序：原生标题 → 即将参加/我发起的/已结束 → 活动列表。移除正文重复页名、口号、通知、攀爬记录。

每条只放一处日期/时间、标题、馆名和状态；复用首页 compact 活动卡变体，当前用户状态替代首页发起人署名，避免本人列表重复自己的头像。待确认明确写出，取消不能伪装成结束。第一条已确认活动可用轻微强调，直接在原列表中强调，不复制成额外“下一场”面板。

长列表自然分页、尾部轻量加载，不用大空态当“加载更多”。空态只有“暂无这类约爬”和找搭子入口；真实服务端分页及历史兼容逻辑保留。

### P02 profile-edit｜编辑资料

顺序：头像/昵称 → 城市/常爬水平 → 选填资料折叠行 → 联络与可见范围折叠行 → 保存。取消“更换头像”旁再次介绍头像用途；头像本身带小编辑标记。

昵称常驻；名片名若与昵称不同显示一行摘要，编辑入口收在选填资料内，保留两个字段原有语义，不自动同步覆盖。头衔、MBTI、身高、臂展、名片一句话放选填组，有值显示简短摘要。等级沿用统一 picker；“换一句”保留一处动作，不同时放骰子和另一个不明含义图标。

联络公开开关必须与实际接口可见范围相符；必要隐私说明就在开关旁，不能为省字删除。保存时隐藏字段原值保留。长表单页面自然滚动，字段错误自动展开并定位，键盘不遮住保存和当前字段。

### P03 friend-list｜岩友

顺序：搜索 → 我的岩友/收到申请/已发申请 → 人物列表。搜索使用输入框清除图标和键盘搜索，删重复的外置“清除”“搜索”按钮，保留实际可用的触发方式。

每人头像、名字、城市/常爬摘要只出现一次；岩友号只在确有识别需求时作为辅助。已有关系显示状态，不再摆同义操作。收到申请一项接受主动作＋拒绝次动作；删除放个人行更多，名片由点击人物进入。

同一个人在当前列表只一个条目；搜索模式替换正文列表，不把搜索结果叠在完整朋友列表上。不改变关系查询或屏蔽权限。长昵称按钮不足空间时操作区另起一行，不压缩成窄按钮。

### P04 circle-list｜岩友圈

顺序：原生标题及一处创建操作 → 圈列表。每圈只保留圈名、最多两行简介、人数/城市合成一行、关系状态。移除“已在”旁第二处加入暗示。圈标色只作用于小图标，不铺背景。

点击整行进详情，申请动作统一在详情完成，列表只显示未加入/申请中/已加入状态，避免列表与详情各一套申请按钮。空态可把同一创建操作放入空态槽，但不能页头页尾同时出现。

### P05 circle-detail｜圈详情

顺序：圈名/简短简介 → 成员摘要行 → 关联岩馆摘要行 → 必要待审批提示。移除顶部“成员/岩馆/城市”统计块与正文重复计数、逐条铺满的馆名标签。

成员摘要显示真实头像和人数，打开单层弹层看完整名单；馆摘要打开已有馆列表弹层。管理者待审批数在正文可见，点开处理，不把全部申请人铺满页。设置内唯一放编辑、关联馆管理、退出/解散。非成员只一个申请按钮，已加入不再显示另一条“大号已在圈内”底栏。

圈主/管理员身份保留，解散与退出仍需明确确认。不新增动态广场。弹层中打开名片时关闭/替换当前层并提供合理返回，不连续叠三层遮罩。

### P06 circle-edit｜新建/编辑岩友圈

顺序：圈标与颜色小行 → 圈名 → 城市 → 简介选填 → 已选岩馆摘要 → 保存。原生标题替代自制返回标题；删除页头“✓”，保存仅一处文字按钮。

已选馆正文最多两条＋“共N家”，点开统一多选 sheet 查看全部，避免正文与弹层都列完整名单。取消弹层保留原选择，保存保留业务必填/权限。圈色数据不改，简介空时不强迫补齐。正文随着展开内容自然增长。

### P07 card-view｜攀岩名片

顺序：完整名片预览 → 一组分享/保存操作，更多位于唯一工具入口。删除“把这张嘴硬存下来”等强设定口号及可同时出现的重复保存按钮。检查原有条件分支，不把互斥平台按钮误删到失去能力。

宽度受容器约束、等比 aspectFit，不裁卡面；短屏缩小预览但提供点开清晰大图，不缩小真实导出尺寸。分享用原生按钮。操作区与图片之间弹性留白即可，不用多张卡片套图。已有图内内容不在图外再全文重复；必要用户名字可用于识别缩略图。

### P08 card-gift｜赠送名片

以当前阶段呈现一条流程：选/准备草稿 → 生成领取信息 → 分享结果。不是新增强制向导；沿用已有状态切换，同一操作区变换主动作。草稿保存、复制等收为次操作，原始 draftCardId/giftId 不常驻显示。

生成前保留短句“本次消耗1次灵感”等实际费用/次数信息，数值按真实规则；不能为精简删掉代价。生成后只展示一份领取码和转发操作，码保持白底和静区。不要同屏继续展示已完成的生成步骤和整段使用教程。

短屏保证二维码完整；可将非必要草稿预览折叠，不能压缩二维码至不可扫。失败不清空草稿，重复点击继续遵循既有幂等/消耗逻辑。

### P09 card-claim｜领取名片

顺序：名片预览 → 简短状态 → 领取按钮。移除“有人塞给你了”“领了就是你的”和“名片预览”重复介绍。标题只在原生导航出现。

领取成功同一操作槽变“查看名片”，保存/分享在 card-view 完成。若现有领取动作包含保存，保持行为并用准确按钮文字，不静默改变用户授权。已领取/过期/无权限/网络失败有不同状态，不能都给可点领取。完整图片按比例显示，长说明可滚动。

### P10 wall｜岩馆名片墙

顺序：馆名＋已有张数 → 名片列表 → 唯一上墙操作。删除“攀岩墙/墙上名片”等重复区块名。容量替换规则只在上墙决策前显示一次，不能消失。

两列缩略图仅在名字和卡片可识别时使用，320px/大字可单列；缩略图外只补姓名，不重复图内头衔和一句话。点击进完整名片。实际条目数不多时不强撑多列高度，无数据不造占位名片。分页加载图片，卡面不裁。

### P11 calendar-overview｜日历

顺序：当前日期范围 → 城市/岩馆筛选＋圈筛选收纳 → 日历主体。删正文岩友圈列表、建圈入口、浮动发布/打卡/刷新三按钮。日历只负责按日期发现活动；当前日期范围/月份以实际支持能力为准，不为样式新增翻月接口。

日期格采用等宽七列，今天/选中/有活动状态分开；以数字场次/点标示，不同时再加一段“活动少到多”装饰说明。必要图例极简，不能完全靠绿色深浅辨认数量。点日期沿用时间轴路径，删除“点日期查看…”常驻教学文字。

圈筛选仍保留，成员圈关系不是冗余内容。打卡入口先落到 records 后再移除这里。头部与整张日历优先完整进入常见视窗，六行或大字体时可自然滚动，不能固定高度裁最后一周。

### P12 calendar-timeline｜当天时间轴

顺序：日期/星期一行 → 岩馆筛选 → 头像列头/时间轴。删“岩友＋N位岩友”重复标题、独立发布按钮和完整报名管理弹层。

点时间块后仅展示识别摘要＋“查看约爬”，进入 plan-detail，所有报名/退出/移除等在那里完成。执行前验证 rawPlanId 到详情 planId 的旧记录映射与私密访问；有不兼容记录先记录阻塞并保留旧入口，不批量删除可用链路。

时间轴是本包允许的二维滚动视图：列头和时间轴使用同一个横向位置，刻度与内容纵向同步；剩余视窗给轴体，不再叠整页外层同方向滚动。短时段不硬塞馆名，点击能看完整区间；不可用 clip 隐藏可操作尾部。不改原坐标计算、时段范围或数据口径。

### P13 checkin｜记录攀爬

顺序：馆名可更换＋日期 → 当前周期/类型切换 → 等级步进列表 → 保存。删除重复“打卡/记录今天…”说明，“去爬其他馆”合并到馆名选择，攀岩墙收进一处次入口，不与提交并排强调。

每行只显示等级、累计/上限和本次数量步进器。首次使用依标签“本次”辨别，不堆段落解释。加减热区88rpx，窄屏可以分两行，不能让等级或数量截断。保存成功在同一区显示摘要与“撤销本次”，不在页头又放撤销。

进入时无馆支持现有馆选择，未选前不可提交；若无馆入口链路缺失，先补视图层选馆适配再迁移日历入口。无周期/上限/失败按真实业务呈现，不新增无周期打卡功能。保持 submissionId、批次撤销与幂等。

### P14 activity-records｜攀爬记录

顺序：准确的统计范围＋最多两项核心统计 → 日志列表 → 唯一“记录攀爬”动作。移除问候语、四列拥挤统计和同屏反复页数说明。其余原统计折入“更多统计”，不删除用户已用信息。

现有“本月计划”与“近30天”不可混成同一时间窗口；统计必须带真实口径，无法核实时保留原标签并标待核实。不能称报名为到场、称搭子计数为认识的新朋友数。

日志行显示日期、馆名、摘要、状态，重复 badge/rightText 若同义只留一处。保留真实分页，页码只在分页组件出现一次。固定新建操作存在时留准确占位，分页栏可以完整滚到按钮上方。

### P15 notifications｜约爬通知

顺序：一处“全部已读”工具操作 → 消息列表。原生页名足够，删除通知用途段落。每行只留标题、岩馆、已提供的时间和未读点，去掉逐条“查看约爬”重复文案，整行箭头引导进入。

未读点有可辨语义，已读文字仍清楚；没有未读时不显示可点的无效批量动作。只能用实际状态确定，不猜数量。长消息至少能展开/进入详情读完整，不能固定行高裁文字；失败有一处重试。

### P16 safety｜屏蔽与安全

顺序：屏蔽名单 → 简短隐私/举报帮助入口。已有名单逐行姓名＋解除，取消“已屏蔽的岩友”与页名的长解释组合。举报仍从具体活动的更多进入，这里不再增加第二个泛举报表单。

隐私提示放可展开帮助，关键权限事实保持可查；没有名单只显示“暂无屏蔽的岩友”。解除保留原确认/失败反馈。管理员举报区不动权限与处置入口，只避免共享样式造成截断；不借本次精简隐藏管理员待办。

### P17 owner｜我的岩馆

顺序：紧凑真实汇总 → 岩馆列表 → 一处添加岩馆。移除“馆长/我的岩馆/岩馆列表”三层标题及“你管理N家，下面是…”句子。

每馆名字/城市/管理入口，列表条目不再重复展示总汇总。空态复用添加操作槽，不能与页头加号同时出现。无权限与无岩馆明确区分。两列统计在窄屏换行，不改线路汇总算法。

### P18 gym-manage｜岩馆管理

用统一三段导航“资料/周期/线路”，每次展示一个独立任务，不再整页堆三份表单与三枚返回按钮。各任务仅一个保存动作，原生返回负责退出；切段保留各自草稿，有未保存内容时不能静默丢失。

资料：基本字段、模式，合并历史折叠，危险操作放末尾更多。周期：当前周期、保存，历史折叠；关闭周期在当前周期管理中。线路：等级表＋唯一更新动作，添加等级是次操作。审核状态就近以短标签显示，必要说明只在提交前一次表达。

三段内容高度自适应，不以固定高容器裁表单；周期历史/等级长列表使用页面滚动。切段不能让底栏误提交另一段。删除/关闭/合并影响说明与确认保留，不能为了短界面移除风险信息。

### P19 gym-merge｜岩馆合并

顺序：源馆→目标馆（一次） → 预览摘要 → 必要周期映射 → 确认。目标选择后不要紧接着又列一遍“当前目标馆”完整卡片。删除“会尽量加载全部可管理岩馆”等实现解释。

源/目标名称必须足以区分；小屏纵向，不截成相同前缀。冲突有文字标记并可查看逐项映射，长映射表纵向展开。目标或映射变化后旧预览不可继续作为确认依据，遵守原流程。

确认前保留数据迁移影响与准确检查结果；条件不足显示具体未完成项。操作按钮固定时确保末条冲突可滚到视野内。本包只设计与测试安全展示，不实际合并数据。

### P20 gym-merge-result｜合并结果

顺序：真实结果状态 → 源馆→目标馆 → 本次迁移摘要 → “进入目标岩馆”。映射明细/合并历史按需展开，目标馆完整资料到目标页查看，不在结果页再复制一整份。

移除“继续合并到该馆”的突出入口及重复返回按钮，返回由导航完成，必要无上一页场景提供一个明确返回入口。失败/部分结果不能套成功图标；长摘要自然滚动。结果数字按真实返回，不展示缺失值为0以伪装成功。

## 5. 文件所有权与任务包

本次只交付计划，不启动 agent。执行时检查最新工作区/未提交差异，不能覆盖并行的详情与发布修改。

| 包 | 页面/职责 | 依赖与重点 |
| --- | --- | --- |
| S0 组件合同 | 共享样式/组件compact变体与状态样板 | 与正在改四页的agent统一接口；唯一共享文件负责人，不再生一套选择器 |
| S1 身份关系 | profile-edit、friend-list、circle-list/detail/edit | 保存隐藏字段、关系状态、成员弹层和管理可达性 |
| S2 日程记录 | calendar-mine、calendar-overview/timeline、checkin、activity-records | 先补记录页打卡入口，再删除日历入口；先验证详情旧ID再去重时间轴 |
| S3 名片 | card-view/gift/claim、wall | 完整预览同源、导出尺寸、扫码、次数与成功状态 |
| S4 通知安全 | notifications、safety | 唯一通知入口留在我的，权限/管理员区不回退 |
| S5 馆长 | owner、gym-manage、gym-merge/result | 草稿不丢、确认不删、长映射完整可滚 |
| S6 整体验收 | 20页及共享影响抽查 | 手机截图、入口闭环、无裁切、同组件一致 |

S0先定义合同，后续可按互不重叠文件执行；不擅自修改在途四页。每包仅修改所列页面 WXML/WXSS/JSON 和必要视图 JS；云函数、权限、接口数据合同不在此包。共享样式新增明确变体，避免全局压缩现有页面。

删除/迁移入口需要更新事件绑定但保留深链接路由。功能缺陷单独记入现有修复计划，不把“视觉去重”当作绕过服务端逻辑的理由。不新增动态 feed、消息能力、统计查询、后台任务或外部依赖。

## 6. 实测交付与通过标准

- 覆盖20页默认态；表单加已填/错误/提交中，名单加长内容/空/加载失败，权限页加不同身份，名片加加载失败/旧图/长文本。
- 每包提供同设备同数据前后截图，记录实际内容高度、滚动容器数、固定栏高度及最后一项可见情况；不能只交“已紧凑”的文字。
- 320/375/430 CSS px 宽度，短屏约568px与常见667/844px整机规格；实际可用视窗扣除由微信处理的区域后测量。字体默认及放大至少一档，iOS/Android各抽查一台。
- 核对默认短筛选值完整、箭头不变形、关键状态不省略、点击区不重叠、底部按钮不遮挡、键盘后可继续操作。列表长行增长，不在中间裁掉一半文字。
- 最后一个等级/成员/历史项/映射项都能完全滚入视野。短页不无故多滚一整屏，长页不强制翻页吸附。时间轴列头与主体对齐。
- 图像按正确比例完整显示，二维码清晰可扫；图内内容无需在小缩略图中可读，但必须有完整查看入口。导出图不因屏幕布局变小而降质。
- 全局入口审计：通知、记录、发布、报名管理等符合归属表；同页不出现重复主操作。状态分支可能互斥，不能只按WXML出现次数机械删按钮。
- 必要本地回归与微信编译通过，分享/保存/返回/私密深链/编辑不丢值抽测。真机、云端未做项明确记录，不以静态代码或浏览器样板冒充。
- 首屏不增加云请求，不用全量拉数据凑人数和头像，不引入图片大Banner、远程字体、持续动画；报告包体和请求数是否变化。

交付格式：页面与文件列表、已移除重复项、入口迁移核验、复用组件、前后截图、实测结果、剩余问题。不得自动上传、部署、迁移或执行合并操作。
