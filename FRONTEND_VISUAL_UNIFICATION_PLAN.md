# 前端视觉统一检查与修改计划

检查日期：2026-09-09。范围：app.json 注册的 35 个页面、全局 WXSS、5 个公共组件及页面 WXML/选择交互代码。

本次为静态代码审查，没有在微信开发者工具或真机逐页截图验证。下文区分已确认的代码差异与需要渲染验证的风险；不把编译通过等同于视觉验收。本次只新增本文档，不修改业务代码，避免干扰另一位 agent 正在进行的调整。

## 结论

基础视觉已明显统一，但尚未完成整体验收。当前页面和组件 WXSS 中的常规文字字号已经基本收敛为 24/28/32/40rpx；背景、按钮、输入、picker 触发器和弹层多数使用 app.wxss。仍存在相同任务使用不同选择方式、同类内容标题层级不一致、页面间距不同和公共状态不完整的问题。

不建议再做一轮全面换肤，应在现有深色、紫色主操作的体系上补齐差异。名片画布、时间轴、日历热度、圈子自定义颜色属于特殊内容，不应机械套用普通表单卡片的尺寸和配色。

## 已确认完成的收敛

- app.wxss 明确了字号 40/32/28/24、按钮 btn-*、表单 input/textarea/field-select、底部弹层 sheet-*。
- 首页、我的约爬和发布页的分段选择使用 seg-tabs；选中态为紫色。
- 原生 picker 触发器已普遍使用 field-select，输入框和触发器均为 28rpx、最小高度 80rpx、圆角 16rpx。
- dateCalendarPicker、gymCard、paginator 已设置 addGlobalClass，不能再把它们误判为完全无法继承全局样式。仍需在运行时检查其实际渲染。
- 新增约爬相关页面已移除各自的 social.wxss import，使用 app.wxss 中的样式。

## 待调整事项

| 优先级 | 问题和证据 | 建议修改 | 验收标准 |
| --- | --- | --- | --- |
| P1 | 城市选择形式不同：home/index.js 的 chooseCity 使用可编辑 showModal；profile-edit/index.wxml 使用原生 picker；calendar-overview、circle-edit 使用底部弹层 | 统一城市选择入口为 field-select 或明确的城市标题入口，打开同一套城市弹层；提供搜索、当前选择和必要的自定义输入 | 同一城市在首页、资料、圈子、日历中可被一致选择；取消保留原值 |
| P1 | 岩馆选择形式不同：首页/总览使用 sheet，calendar-timeline 使用原生 picker，calendar-publish 在正文中展开搜索和列表 | 岩馆长列表统一搜索弹层；发布页默认显示已选岩馆摘要或选择入口，避免搜索结果撑长表单。保留各页单选/多选业务区别 | 统一搜索框、列表行、地址辅助文字、选中标记及加载/空/失败状态 |
| P1 | 首页岩馆弹层只有文字和加载更多，没有选中标记，也没有明确无结果提示（home/index.wxml）；field-select 的文字缺少独立收缩/省略容器 | 补选中态和无结果/失败重试；增加 field-select__value，负责 flex:1、min-width:0 和省略，箭头保持可见 | 长岩馆名不会挤掉箭头；空搜索结果有解释；返回后能识别已选项 |
| P1 | 相同约爬标题：首页 plan-card__title 为 32rpx/800，我的约爬使用 social-name 为 28rpx/700 | 新建约爬卡片标题语义样式，首页和我的约爬共用；人名继续使用 social-name | 同一活动在两个列表的标题层级相同；长标题可合理换行 |
| P1 | 开关大小不同：首页 availability switch、资料页 contact-switch switch 缩放 0.8；发布页审核开关未缩放。发布页类型多选为 tag，氛围为原生 checkbox 行 | 统一开关尺寸与行布局；类型/氛围采用同一多选视觉规范，同时保留正确的选中绑定和点击反馈 | 三处开关视觉大小一致；多选区选中/未选中颜色、字号、点击区域一致 |
| P1 | 页边距：page 左右 24rpx，social-page/首页 28rpx；首页顶部 36rpx、普通页 28rpx；social-title 行高 1.4，page-title 1.3 | 以普通页面左右 24rpx、顶部 28rpx 为默认，主标题统一行高；首页特殊标题区域如保留差异需明确记录 | 首页、我的约爬、发布、详情、通知、资料页面边缘对齐；特殊布局有明确理由 |
| P1 | social-page 无条件保留 200rpx 底部空间，通知/安全/我的约爬也使用它；action-bar 和 social-bottom 的安全区前基础留白分别 20/24rpx | 页面基础容器与固定底栏占位拆开，固定栏共用 action-bar；按是否有固定操作栏分配底部空间 | 无固定栏页面没有大段人为留白；有栏页面最后一项不被遮挡；真机安全区正常 |
| P2 | styles/social.wxss 仍保留旧的 42/27/26/29/25rpx 规则，但当前未发现引用；app.wxss 中 social-* 所谓别名仍是独立定义，如 social-label 与 field-label 色彩/间距不同 | 确认其他在途改动不依赖旧文件后移除；同语义样式合并规则，保留步骤标题与普通字段标签的必要区别 | 无旧文件引用、无重复维护的同义样式，新增页面不会重新引入旧字号 |
| P2 | 颜色语义：分段选中紫色，sheet-item__check 是金色，底部 TabBar 选中也是金色；所有我的约爬状态都用 tag-warn | 明确“主操作/选中紫色、提醒金色、危险红色、中性灰色”；状态按待确认/已确认/已结束/取消区分；TabBar 金色若作为品牌例外需记录 | 同一种状态在首页、我的约爬和详情一致，状态同时带文字，不仅依赖颜色 |
| P2 | 发布页 cal-cell、总览 cal-cell、dateCalendarPicker 日期格尺寸不同；有不同业务内容，不能直接判定为错误 | 统一圆角、文字和今天/选中/禁用状态规则；允许带星期/热度的格子更高 | 今天与选中可区分；发布页和日期弹层的选中态一致 |
| P2 | empty-state 基础已统一，但首页 list-status、通知纯文本空态、我的约爬“向上滑动”使用大块 empty-state；首页空态与固定栏同时有主要发布按钮 | 区分首屏空态与列表尾部状态，统一 empty-state/list-status 职责；首页空态保留一个主要发布入口 | 加载更多不占大块留白；空态有清晰引导且不重复主操作 |

## 需要实际渲染验证的风险

1. 首页双筛选框加“日历”按钮：窄屏下是否挤压长岩馆名、箭头或文字按钮。
2. 发布页时间字段：开始/结束文字加时间与箭头，在窄屏和系统大字体下是否截断。
3. sheet 的 max-height + flex + scroll-view：长列表是否真正滚动、搜索键盘是否挡住结果；并验证弹层顶部/底部留白。不能仅据 WXSS 判断必然失效。
4. sheet 与 sheet__ft 均含安全区 padding：有 footer 的弹层是否出现双倍底部空间。
5. 通知标题加“全部已读”、待确认昵称加两个文字按钮：长文字能否合理收缩/换行，按钮是否被挤窄。
6. activity-records 四列统计卡和 paginator：小屏下数字、标签和分页按钮是否拥挤。
7. 全局 button reset 对原生禁用/loading/share 按钮的影响；btn-text 的 disabled 态也需专门核对。
8. 资料页、我的页、分享/保存页的名片属于 Canvas 输出，需直接比对生成图片，不能用 WXSS 字号扫描代替检查。

## 执行顺序

### 第一批：建立可比较的基线

- 等待当前样式修改稳定，记录 Git 提交或工作区快照；不要覆盖另一位 agent 的未完成改动。
- 在同一设备、同一账号、同一批数据上截取首页、我的约爬、发布、详情、我的、资料、通知、圈子/日历的默认态与选择弹层。
- 以现有 app.wxss 为基础确认字号、色彩、边距和控件规格；记录必要例外。

### 第二批：修用户主流程（P1）

- 先统一城市/岩馆选择，再统一约爬卡片标题和长文本处理。
- 再统一开关、多选控件、页边距和固定底栏。
- 共用选择组件只负责 UI、选中值和事件；搜索请求、分页与业务权限仍由服务/页面处理，避免视觉重构改变业务行为。
- 不强行把时间/日期原生 picker、危险操作确认框也替换成搜索弹层；统一依据是相同任务，而非所有控件必须同型。

### 第三批：收敛与防回退（P2）

- 清理无引用样式，合并同义规则，统一颜色和页面状态规范。
- 后台页继续沿用公共表单和按钮规范，允许更紧凑的信息布局。
- 文档记录常规字号和受控例外；检查项针对重复样式、非规范普通文字字号和旧样式引用，避免误报 Canvas、图标和时间轴。

## 验收清单

- 35 个注册页面逐页默认态检查；核心 8 页覆盖空、加载、错误、长文字与相关身份状态。
- 在 320/375/430 CSS px 宽度的模拟设备检查，并在 iOS/Android 真机抽查原生 picker、开关、弹层、键盘与底部安全区。
- 测试长城市名、30 字岩馆名、40 字活动标题、长昵称、空列表和多页搜索结果。
- 同类标题字号一致；主操作和次操作清楚；选择器当前值、箭头、选中态可辨识。
- 无横向溢出、截断关键文字、底栏遮挡、不可滚动弹层和重复主操作。
- 运行微信 WXML/WXSS 编译校验，再复核截图；本轮规划不执行上传。
- 完成后保存前后对照截图和未解决项，才能宣称视觉统一完成。

交付边界：本计划仅涉及视觉和对应选择交互的统一，不扩展社交功能，不修改云函数、数据模型或发布线上版本。
