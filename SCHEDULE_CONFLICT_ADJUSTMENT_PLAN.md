# 同时段约爬互斥调整计划

日期：2026-09-10  
优先级：P1，体验版发布前修复  
状态：仅方案，尚未实现；本文件不表示已修复或已部署。

## 1. 问题与目标

当前 `calendar_plan_publish/index.js` 的创建/编辑和 `lifecycle.js` 的报名/审批围绕单个约爬校验权限、人数和状态，缺少同一用户跨约爬的时间冲突校验。即使两个约爬各自事务成功，也可能让同一用户同时出现在两个局中。

目标：同一用户在有交集的时间段内，只能组织或参加一个约爬。覆盖组织+组织、组织+参加、参加+参加，所有城市、岩馆和可见范围使用同一规则。身份由服务端登录态确定，不接受客户端传入的身份作为判断依据。

本轮只补齐约爬业务约束和必要反馈，不增加新的导航入口、筛选项或个人日程页面。**保留当前“日期 + 上午／下午／晚上（可多选）”的发布方式，不恢复具体开始/结束时间输入、自定义时间或新增时间确认步骤。** 下文时间区间仅用于服务端判断和兼容旧数据，不是新的用户输入要求。

## 2. 产品规则

### 2.1 时段选择与冲突边界

- 用户继续只选择日期和上午/下午/晚上。新约爬以 `timeSlots` 为占用依据：同一天所选时段有交集即冲突，不同日期或没有交集则允许。
- 例如：已有“周五下午”，不能再发起或参加“周五下午”或“周五下午、晚上”；可以参加“周五上午”或“周五晚上”。
- 多选表示该约爬占用全部所选时段。选择“上午、晚上”只占用这两个时段，**不得把中间未选的下午锁住**。不能直接使用最早开始到最晚结束的外包范围判断冲突。
- 当前 `miniprogram/utils/plan.js` 定义上午 10:00–14:00、下午 14:00–18:00、晚上 18:00–22:00；沿用此映射，不在本轮调整产品时段边界。前后端采用同一版本的定义，服务端自行归一化和验证 `timeSlots`，不能信任客户端上送的派生时间。
- 服务端将所选时段转换成区间数组，每段为 `[startAt, endAt)`；两份安排任意一对区间满足 `a.startAt < b.endAt && b.startAt < a.endAt` 即冲突。相邻时段可参加，不添加通勤缓冲，不限制一天只能参加一场。
- 老数据无 `timeSlots` 时，以原有实际起止区间参与比较。例如旧局 13:00–15:00 与新局“上午”或“下午”均冲突。不能简单把旧数据舍入到某一个时段导致漏判。
- 统一按北京时间生成区间。服务端拒绝空/非法时段；有 `timeSlots` 的新请求由服务端派生兼容起止字段，忽略或拒绝不一致的客户端时间，防止伪造时间绕过。无时段的旧客户端走显式旧数据兼容分支，不开放新的自定义时间界面。
- 沿用现有“不跨日”发布规则；历史跨日记录必须覆盖涉及的每个日期，不能只检查开始日期。兼容 `startTime/endTime` 字段可以保留给旧展示、排序等逻辑，但不能代替多段占用；执行时核对截止和结束规则与多选状态一致。

### 2.2 哪些状态占用时间

| 身份/状态 | 占用个人时间 | 处理 |
|---|---|---|
| 有效约爬的发起人 | 是 | 创建即占用，取消或结束后释放 |
| 已参加/已确认（joined、confirmed） | 是 | 报名或审批成功后占用 |
| 待确认（pending） | 是，作为本计划的推荐默认规则 | 申请成功即预留个人时间；不改变“待确认不占活动名额”的现有规则 |
| 已退出、已拒绝、被移除 | 否 | 对应状态变更成功后释放 |
| 整场已取消或已结束 | 否 | 所有成员的占用失效 |

选择 pending 预留时间，是为了避免用户向多个重叠局申请后被陆续批准。用户需要先撤回原申请，再申请冲突时段的另一场。申请提示使用“申请后将预留此时段”，只在需要确认申请的交互中出现一次，不在各页重复铺设说明。

待确认的失效时点必须统一：以服务端现有截止规则为依据，超过有效申请截止时间后，pending 不再占用且不得再审批通过；不得出现“锁已释放但申请还能通过”的状态。若现有审批允许截止后通过，执行时应将该规则同步收紧，并覆盖通知和“我的约爬”的状态展示。已确认成员不会因为报名截止而失去占用。

同一用户同一约爬只有一个有效占用，发起人不重复计入；重复点击、请求重试不产生第二条占用，也不能将自己原来的占用误判为冲突。

### 2.3 改期

- 仅发起人、没有其他有效 pending/confirmed 成员时：允许修改日期或所选时段，原占用释放与新占用建立必须原子完成；冲突失败时保持原选择。
- 已有其他有效成员时：本轮禁止直接修改日期和时间，提示“已有岩友参加或待确认，暂时不能改期”。仍允许修改不涉及时间且原有权限允许的字段。
- 这是避免未经成员同意改期、以及一次事务修改大量成员日程的明确产品取舍。本轮不扩展改期征询功能；不要自动退出成员或替换其其他约爬。

## 3. 服务端实现方案

### 3.1 共用服务与写入入口

在 `cloudfunctions/calendar_plan_publish/` 增加独立的时间占用模块（建议 `schedule.js`），统一时间归一化、冲突比较、占用/释放和错误返回；创建、报名、审批不得分别复制规则。

| 入口 | 必须处理 |
|---|---|
| create | 检查发起人日程，事务内创建约爬及占用 |
| update | 排除当前约爬；检查其他成员及新时间，事务内替换占用 |
| join_plan | 检查报名者日程；direct 建确认占用，approval 建待确认占用 |
| approve_joiner | 检查申请有效性、截止时间、名额和被审批者日程；pending 原子转 confirmed |
| unjoin_plan / reject_joiner / remove_joiner | 更改成员状态的同时释放其占用；只释放目标约爬 |
| cancel / 管理或举报触发的取消 | 整场失效，所有相关成员不再被此局阻挡 |
| 再约一场、预填发布、旧客户端调用 | 最终仍经过上述服务端入口，不能绕过 |

执行前再次检索所有约爬及报名写入位置，尤其 `community.js` 中的管理取消，确认无旁路。不要将打卡记录、历史攀爬记录误纳入实时约爬占用。

### 3.2 并发保护是必要条件

不能只“先查询其他局，再新增报名”：两个请求同时查询到空闲仍会一起成功。仅给两个不同约爬各自加事务也不能解决。

建议新增仅云函数可写的 `RockUserScheduleDays`，文档 ID 使用服务端生成的用户标识哈希与北京时间日期，存储该用户当天的约爬引用、所选时段及归一化区间数组、状态和必要版本信息。同一用户同一天的所有占用写操作必须读写同一文档，使并发创建/报名产生事务冲突并重试。

一次事务应完成：读取日程及目标约爬/报名 → 校验权限、截止、容量和占用 → 更新日程、约爬、报名及必要计数。不能先提交报名再异步加锁。对不存在的日程文档，也必须证明并发初始化只有一个写入能成功；采用确定性 ID，禁止随机 ID。

多日占用按日期固定顺序读取/写入。事务冲突采用有限重试，重试时重新读取并判断；超过上限返回可重试失败，不得按成功处理。发送通知等外部副作用在事务提交后触发，保持已有幂等机制。

具体 SDK 事务能力、文档大小、读写上限及创建冲突行为，需要执行 agent 在落地前核实。验收必须包含真实云环境并发，不能仅凭单线程 mock 宣称互斥成功。

### 3.3 释放、取消与一致性

- 单个成员退出/被拒/被移除：在同一事务中释放其日程。
- 整场取消涉及多个用户，不应无界地在一个事务里重写所有日程。以约爬取消状态为权威：后续占用检查必须在事务内核验可能冲突条目的约爬有效状态，忽略并清理已取消记录。异步清理只用于减少数据量，不决定是否允许参加。
- 取消与新报名竞争时，新报名必须读取目标约爬状态；取消与另一场报名竞争时，新占用判断必须读取旧场状态。发生事务竞争后重新判断，保证取消成功后不会长期被旧锁阻挡。
- pending 到期同理：按统一截止规则实时判定失效，不依赖定时任务及时运行。
- 清理过期日程可作为后续维护任务；不可无限追加历史条目到同一文档。

### 3.4 统一错误合同

- 时间冲突返回 `SCHEDULE_CONFLICT`，可向本人返回其冲突约爬的合法可见引用和时间；客户端仍需重新校验详情访问权限。
- 普通提示：“周五下午你已有约爬”；操作为“查看约爬”/“调整时段”或“知道了”，按当前页面择一，不堆叠按钮。用户界面使用日期与时段名称，不为新记录显示内部派生的具体钟点。
- 审批他人失败只提示“该岩友此时段已有安排”，不要向发起人泄露对方其他私密约爬的岩馆、成员等信息。
- 改期受限使用独立错误码 `PLAN_TIME_LOCKED`。其他网络、数据库异常不得伪装成冲突或空闲。

## 4. 前端调整

- 发布页：保留日期及“上午／下午／晚上”选择控件，不新增必填项或具体时间选择器。冲突在时段区域显示一条反馈并标识冲突选项，保留已填写内容及其他选择；用户自行调整时段后重试，不能静默删掉多选中的冲突时段。
- 详情页：申请/参加失败后还原按钮加载状态，不增加人数、不显示成功、不跳转。本人已参加当前局时继续展示现有管理状态。
- 审批入口：失败时保留正确的申请状态，显示简短反馈，不误增人数。
- 首页、详情、“我的约爬”统一处理成功后的缓存失效；失败不得提前修改本地参与状态。退出、取消后允许立即报名另一个重叠局。
- 可选前端预检查用于提前反馈，但不能代替最终服务端事务校验。本轮不必为此新增常驻提示、日程页面或全列表逐卡请求。
- 使用已有表单错误、弹窗、按钮和字号规范，适配窄屏长错误文案，不挤压主操作。

## 5. 旧数据与上线顺序

现有约爬不会自动出现在新日程表中，不能创建空表后直接声称限制生效。

1. 先只读审计尚未结束的约爬及有效报名：统一 owner 身份、旧 joined/confirmed、重复报名的最新有效状态、取消和 pending 到期规则；完整分页，不能只取最近 200 条。
2. 输出异常清单：已有时间冲突、无法解析的时间、成员索引与报名不一致。只统计必要字段，避免输出私人资料。
3. 编写可重复执行、有版本标记的回填及校验工具，先 dry-run，备份后再在独立批准的上线窗口执行。本文只要求编写方案，不授权现在迁移数据。
4. 回填时必须阻止并发写入造成漏记，可使用服务端统一写入维护开关；旧客户端同样受服务端开关约束。先上线全部入口的开关能力，再冻结相关写入、回填、校验、启用新约束并恢复写入。
5. 已有重叠记录全部保留并如实纳入占用，不自动替用户退局或删除。禁止新增冲突，用户可退出/取消来消除历史冲突；无法判断的记录需修正或明确隔离后再启用相关写入。
6. 前端与云函数都部署后，再做双账号验收。回滚应保持限制写入或维护状态，不能只回滚约束云函数让旧入口继续制造冲突。

普通报名只读取当天日程及少量候选约爬，不扫描用户全部历史或全站约爬。记录事务重试与冲突数，避免在日志中写出其他人的安排详情。

## 6. 执行拆分与交付

| 阶段 | 工作 | 完成条件 |
|---|---|---|
| S1 | 统一状态、时间、截止及错误合同，列出全部写入入口 | 产品规则和旧数据审计结果明确 |
| S2 | 共用占用模块及 create/update/join/approve 的事务接入 | 三类角色组合均受限，并发只有一个成功 |
| S3 | 退出、拒绝、移除、各类取消和到期的释放机制 | 释放后可立刻加入，无错误释放他局 |
| S4 | 发布/详情/审批的最小反馈与缓存同步 | 失败保留输入，状态不误报，无新增重复入口 |
| S5 | 迁移工具、回归、真实并发与部署清单 | 完成以下验收并记录证据，再允许体验版上传 |

S1–S3 建议由同一执行 agent 负责后端，避免事务规则分叉；S4 可在错误合同固定后交给另一个 agent。验收者检查最终差异和云端证据。不得顺便重做页面或扩展社交功能。

## 7. 验收用例

- 组织+组织、组织+参加、参加+参加分别测试完全重合、部分相交、包含、端点相接；跨岩馆/城市/私密范围同样受限。
- 新时段用例：同日相同时段拒绝、相邻时段允许、不同日期允许；“上午+晚上”不阻挡下午，但阻挡上午或晚上；“下午+晚上”与下午发生冲突时整次请求失败，不得部分报名。
- 新旧兼容用例：旧局 13:00–15:00 阻挡上午和下午；伪造 `timeSlots` 对应的派生时间不能绕过限制；前后端时段定义一致。
- 同局重复请求幂等；仅自己原来的占用不会阻止编辑非时间字段。
- pending 阻止另一场申请/发起；撤回、拒绝、移除及到期后允许参加；截止后审批不能恢复失效占用。
- 已确认报名在申请截止后仍占用；整场结束后不阻止后续活动。
- 两台客户端以同一账号同时发起两局、同时参加两局、同时发起和参加：存在交集时只能成功一次；失败请求不遗留计划、报名、人数或通知副作用。
- 两个发起人同时审批同一岩友、审批与退出竞争、取消与报名竞争、第一次创建日程文档竞争，结果均一致。
- 没有其他成员时改期成功原子替换；冲突改期失败保留原时间；有其他有效成员时改期被拒，非时间字段仍按原规则可编辑。
- 旧 joined/confirmed、重复报名终态、未回填用户、超过 200 条历史数据、旧跨日数据和非法时间均有覆盖。
- 模拟事务失败、超时和重试耗尽：没有部分成功；前端可重试且不丢表单内容。
- 现有回归全部通过；新增 `tests/schedule-conflict.regression.js` 覆盖真实业务不变量，另记录云端并发结果。仅 mock 测试不能替代云端并发验收。
- 小屏检查发布冲突提示、详情失败和审批反馈；确保文字完整、主操作可见。确认发布仍只用日期与上午/下午/晚上，未恢复具体时间输入。

## 8. 发布判定

这项限制在服务端互斥、释放链路、旧数据接入和真实并发验收全部完成前，保持“未解决”。当前 2.0.25 准备中的其他修复不包含此能力，不应将其作为已通过本项验收的版本上传。
