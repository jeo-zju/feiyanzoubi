# 首页与“我的”样板第二轮调整

日期：2026-09-10。状态：设计修订，供执行 agent 使用，本次未修改页面代码。

依据：用户提供的首页微信截图，以及当前 home/me 的 WXML、WXSS 和 user-theme.wxss。首页问题有截图证据；“我的”未提供截图，其建议依据当前结构及用户反馈，待同设备实图验收。

本文件优先于 FRONTEND_REDESIGN_EXECUTION_PLAN.md 的 V01、V05、首页底栏和对应样板要求；其他页面保持原计划。保留暖黑、粉笔白、酸橙绿和时间签的方向，修正密度与主次，不启动另一轮换色。

## 1. 本轮要解决什么

首页：

- 城市与通知一行、双行口号、四段日期、两个框＋日历、独立开关，连续五层控制与标题把活动推到下方。
- 通知未贴右、日历独占大片横向空间；“全部岩馆/全部类型”这种短默认值也被截成“全部…/全…”。这不是正常的长文案省略，必须解决布局占宽。
- 卡片把标题、岩馆、头像、姓名逐行堆左边，名额则单独落到最下方；右侧中段缺少对应信息，形成不平衡，而非有意留白。
- 独立固定发布区较高，按钮没有利用可用宽度，页面底部又有多段留白。需要在微信视窗中核对按钮默认样式、容器高度和安全区，不能仅凭截图认定是哪条 CSS。

“我的”：完整身份头部后紧跟一张完整名片，再跟两枚按钮、多个菜单分组和标题，重复展示个人信息且纵向过长。当前 menu-group 间距 40rpx、menu-row 最小 112rpx；名片完整比例与多个组叠加是主要高度来源。me-page 在原生导航下另加顶部安全区，页尾又有安全区占位，需要实测是否重复。

原方案的双行首页大标题和“我的”完整名片展示在这一版中让位于浏览效率。设计感保留在排版、时间和人物信息上，不需要每屏都有海报级标题。

## 2. 首页新结构：四层就进入活动

### A. 顶部城市与日历（第三轮反馈更新，2026-09-10）

用户确认移除首页通知，通知仍从“我的”进入。右侧替换为“日历视图”，复用已有 calendar() 路由；它提供另一种查看约爬的方式，不新增业务功能。同步移除筛选弹层中的日历入口，避免重新造成重复。

```text
杭州⌄                         日历视图
找个人，一起爬。
近期       今天       明天       周末
```

- 左城市 30–32rpx/600，右侧使用小日历线性图标＋“日历视图”26rpx/500，中性浅灰，无实心底、无描边、无外置箭头；没有统一图标资产时纯文字也成立。
- 顶部整行 88rpx，两个入口的视觉中心在同一水平线上，左右分别与下面卡片边缘对齐。原城市 `.city-entry` 使用 baseline 且 min-height:88rpx，文字偏上；改为 align-items:center，内部文字控制行高，不要用 padding-top 硬推。
- 右入口明确 width:auto、flex:0 0 auto、margin:0、合理 padding 和 box-sizing。当前 `.notice-btn` 没有显式 width，微信运行时是否受默认宽度影响需用元素尺寸验证；截图中它落在偏中间，不能只替换文案就算完成。
- 可令右入口 width:160rpx（含点击热区）作为可预测的本地布局，左城市 flex:1/min-width:0；文字在右入口内右对齐。长城市名仅左侧省略，不能把右入口挤出屏幕。热区与城市不重叠。
- 页面顶内边距 8–12rpx；顶部行到单行标题 0–4rpx，标题到日期 16rpx。保留 88rpx 热区，通过正确居中和去掉额外间距压缩空白，不缩小到难点。
- 调用现有 calendar()，不承诺自动继承首页全部筛选。当前方法只导航到 calendar-overview；若以后需要跨页同步条件，另列交互改动，不在视觉包静默新增。
- 验收截图：城市和日历同一视觉中心线；日历入口右缘对齐卡片内容区外缘；无按钮默认宽度形成的中间悬空；顶部不新增标语、天气、人数或第二个发布按钮。通知仍能从“我的”打开。

### B. 一句短标题

- “找个人，一起爬。”改为单行 40rpx、字重 600/700；标题与日期筛选之间 16rpx。320px 宽或字体放大时可换行，自然撑高，不强制截字。
- 不添加副文案、封面图或右侧装饰填空。普通字号下正文顶部到第一张卡的目标高度约 300–340rpx（不含系统导航）；这是设计目标，超出须解释。

### C. 日期快捷行

- 保留近期/今天/明天/周末，整体高度约 80–88rpx，单项等宽。
- 选中项弱绿底，未选无描边，整组底色与页面接近；不要把四个标签做成四个大按钮。
- 此处使用首页局部 compact 变体，不全局压缩其他页 segmentedTabs。

### D. 筛选工具行

- 只保留两个入口：左侧“全部岩馆⌄”，右侧“筛选”加图标/数量标记。
- 左侧 flex:1、min-width:0，右侧固定约 144rpx、不可收缩，中间 16rpx。左侧至少允许完整显示“全部岩馆”，右侧完整显示“筛选”；不要均分三个格子。
- 两者视觉高度 72–80rpx、有效点击热区至少 88rpx；弱底色、无重边框。长馆名才省略，完整当前值可在岩馆选择弹层查看。
- 类型与“仅看有名额”移入筛选弹层，日历入口仅位于首页顶部，不在弹层中重复。
- 筛选弹层：标题/关闭 → 类型单选 → 原 onlyAvailable 开关 → 重置与应用。该弹层持有草稿值，取消不改变列表；应用一次更新筛选并请求，不随每次选择反复加载。
- 生效后用“筛选·2”等计数提示，按类型、开关两个条件计数，不包括左侧岩馆和上方日期。原 onlyAvailable 语义沿用当前功能合同，不由视觉包改为另一套“可报名”逻辑。
- 弹层可显示已选摘要；首页无需为每个条件增加一行 chip。希望高频切换类型时可再评估，第一轮不同时保留类型 picker 与筛选弹层两个入口。

## 3. 活动卡片：信息横向成组，不为填空加内容

推荐布局：

```text
周四 · 09.10                 14:00–22:00
一起爬，认识新岩友
嘻呐攀岩（滨江银泰店）
抱石 · 休闲爬                 ← 有真实标签时显示

[头像] 猫老师                    已有 1 人
       常爬 V2 · 5.8             查看约爬 ›
```

这里的类型/氛围是结构示例，用户截图未提供这两个值，不得把示例文案写入真实活动。已知人数上限时，右侧改为“1/4 人”＋“还可加入 3 人”；未知上限显示“已有 1 人”，不猜成四人活动。

- 日期与时间继续一行左右对齐；时间是主信息，但一般用 34–36rpx，不必比标题更重。
- 标题/馆名使用全宽：标题最多两行 32rpx，馆名 26–28rpx 最多两行。完整馆名在详情可读，默认不强制半幅。
- 类型与氛围合成轻量文字行；缺氛围不渲染空行，未知类型如现有“ 一起攀岩 ”降为辅助说明，不当作醒目标签。
- 下方一行左侧是头像、姓名和水平，右侧是名额/状态＋箭头，左右视觉权重建立关联。头像 64rpx；右侧 max-width 180rpx 左右，根据文字换行，不能挤掉左侧姓名。320px/大字体时该行允许右侧另起一行。
- 去掉现有底部独立标签/人数行及其分隔线；时间签下的分隔线也可去掉，仅留 16–20rpx 间距。卡片内边距 24rpx、卡间 20–24rpx，常规短内容目标高度约 290–330rpx，长内容自然增高。
- 整卡可点击，右侧“查看约爬 ›”是提示不是第二个按钮；若版面紧，保留箭头即可。不要添加成员头像堆、地图、假热度或装饰画去占右侧。
- 已满/截止用右侧状态替换剩余名额提示，不把整卡文字透明化。无法报名仍能查看详情。

目标是在相同设备上明显减少卡片中部空洞，并让首屏可见至少一张完整活动卡及后续列表开头。没有第二条数据时不伪造占位活动，也不拉高第一张卡撑屏。

## 4. 发布按钮与底部留白

- 本轮保留固定底部“发起约爬”，不额外在标题右侧增加发布按钮。
- 按钮显式 width:100%、box-sizing:border-box，页面两侧与卡片对齐，96rpx 高；禁止依赖 button 默认宽度形成居中的半宽按钮。
- 操作容器上下各约 12–16rpx；底部安全区由一处处理，不能同时由原生 Tab、操作栏、页面 padding 重复补偿。先检查微信实际 windowHeight/安全区及编译器样式，再确定最终值，不盲目硬减固定像素。
- `.discover-page` 当前固定 200rpx 底部 padding 改为与真实操作区高度对应的占位；Tab 占位按原生实际布局处理。最后一项不被遮挡，也不在按钮下方留出一整块无用空间。
- “近期约爬都在这里了”改为轻量“没有更多了”，与列表尾相距约 24rpx；若仅一项亦不人为居中填满剩余屏幕。

## 5. “我的”：先知道是谁，再去做事

主结构：

```text
[头像] 猫老师                     编辑
       杭州 · 常爬 V2 / 5.8

[小名片缩略图] 我的攀岩名片          ›
              查看、分享与保存

          我的岩友  |  岩友圈

攀爬记录                            ›
约爬通知                            ›
屏蔽与安全                          ›
我管理的岩馆                        ›
管理工具（有权限时）                  ›
```

- 身份区头像 88–96rpx，名字 36rpx；副信息最多两行。常爬信息只用已获得且可显示的资料；缺失时显示城市即可，不新增请求获取“设计所需数据”。岩友号放名片详情或身份第二行按宽度降级，不与姓名抢焦点。
- 名片默认改成高度约 112–128rpx 的入口条，缩略图保持 960:606 比例，约 128×81rpx，旁边是标题和说明。缩略图仅预览，图内小字不承担阅读任务。
- 点入口后打开已有 card-view 路由（当前 me 的分享逻辑已有 cardId 路径可参考）。完整名片、保存和分享在完整预览里完成；必须核对分享/保存动作是否都可达，缺哪个就迁入哪个，不能为了压缩页面删能力。
- 未建卡时同一个入口变为“创建我的攀岩名片”，进入资料页；同步中/失败保持相同高度，不再展示一张大尺寸骨架。
- 原离屏 Canvas 与导出尺寸保持不变，只缩小在页内的预览表现；不要把导出图也缩成小图。修改 myCardCssW/H 之前检查生成、测量及缓存的用途。
- 岩友/岩友圈改为一个扁平双入口条，高约 96–112rpx；两等分、细分隔，可有统一小图标，不做两张大统计卡、不加虚假人数。
- 其余功能合并为一组，行高约 88–96rpx；删“我的攀爬”“账户与安全”等只包一两行的分组标题，入口名字已足够解释任务。组间 24rpx。
- 馆长入口保留当前可达性；只有已明确的权限字段才能用来隐藏管理工具，禁止仅凭 canSeeToolbox 判断全部馆长资格。岩馆合并收回已有管理抽屉，避免首页重复列同类管理入口。
- 版本号放抽屉或页尾的小号文字；普通“我的”不为版本号预留 48rpx margin 再加大 footpad。原生导航已经处理顶部时，不重复添加状态栏安全区。

高度预算（普通字体、常规内容）：身份约 128rpx、名片约 128rpx、岩友条约 112rpx、四项菜单约 384rpx，加页边与组间约 100rpx，总目标约 850rpx。管理用户允许更多内容；320px 或字体放大允许自然滚动，不为“一屏”缩小到不可读。

## 6. 执行范围与顺序

1. 先修首页 button/筛选区域的占宽与固定栏尺寸，确认默认短文案完整，这是布局缺陷，不要用更短的“全…”掩盖。
2. 调整首页头部与筛选弹层；卡片合并人物/名额行，移除多余 footer。
3. 压缩“我的”完整名片为入口，合并菜单分组；检查完整名片页面的分享、保存仍可达。
4. 用与用户截图同一组活动数据回拍首页，并补“我的”截图，再在宽屏/小屏/大字体验证。

主要文件：home/index.wxml、index.wxss、index.js（仅筛选草稿/应用等视图逻辑）；me/index.wxml、index.wxss、index.js（名片入口及显示测量）；必要时 card-view 与 styles/user-theme.wxss。

共享卡片样式被其他页使用时加首页 compact 变体；不全局改 `.plan-card` 让详情/我的约爬一起缩小。修改前检查当前工作区和其他 agent 在途改动，只提交本包差异。

不修改云函数、报名合同、数据模型，不添加新查询，不上传或部署。本包是原 D0 样板的复修，先完成这两个页面再继续其他页铺开。

## 7. 验收清单

- 375px 常规字体下，默认“全部岩馆”和“筛选”完整，日历视图贴右且与城市垂直居中；按钮不会占据莫名其妙的半行空位。320px 和长馆名允许有规则省略，但当前值在弹层可读。
- 首张活动卡开始位置比用户提供样板明显上移；记录导航以下到卡片顶边的实际 px，不只报告 rpx 目标。
- 卡片右侧中部承担名额/状态信息；没有新增虚构信息，人数不知上限时仅显示已有人数。
- 截止/满员/待审批等状态仍真实；列表点击、筛选、取消筛选、应用、重置、日历入口都可用。
- 发布按钮全宽对齐卡片，按钮与 Tab 之间没有重复安全区空洞；长列表末项不遮挡。
- “我的”常用入口在一次快速扫视中可见，完整名片按需打开；分享/保存/编辑/管理权限不回退。用前后截图和实际滚动高度证明变短。
- 微信截图优先；若只用浏览器样板，必须另列原生 button/picker/switch/TabBar 的微信待验收项。截图与源码差异需核查当前构建版本，不凭旧截图反复补 CSS。

交付：两个页面前后截图（相同设备与数据）、关键状态截图、文件差异、实际高度对比及未验证项。本次只交付该计划，未执行修复。
