const {callCloud}=require("../../services/cloud");const {ensureAppLogin}=require("../../utils/session");
Page({data:{blocks:[],reports:[],isAdmin:false,loading:false,error:""},onShow(){this.load();},async onPullDownRefresh(){try{await this.load();}finally{wx.stopPullDownRefresh();}},async load(){this.setData({loading:true,error:""});try{const u=await ensureAppLogin();const r=await callCloud("calendar_plan_publish",{action:"blocks"},{silent:true});const isAdmin=u.role==='admin';this.setData({blocks:r.list||[],isAdmin});if(isAdmin){const reports=await callCloud("calendar_plan_publish",{action:"reports"},{silent:true});this.setData({reports:reports.list||[]});}}catch(_){this.setData({error:"加载失败，请重试"});}finally{this.setData({loading:false});}},unblock(e){const target=e.currentTarget.dataset.id;wx.showModal({title:"解除屏蔽？",content:"解除后可以再次发现和报名对方的约爬。",success:async r=>{if(!r.confirm)return;try{await callCloud("calendar_plan_publish",{action:"unblock",targetOpenid:target},{silent:true});await this.load();}catch(_){wx.showToast({title:"操作失败",icon:"none"});}}});},resolve(e){const reportId=e.currentTarget.dataset.id;wx.showActionSheet({itemList:["不予处理并结案","下架约爬并结案"],success:r=>{const resolution=r.tapIndex===0?'dismiss':'cancel_plan';wx.showModal({title:"确认处理举报？",content:resolution==='dismiss'?'将记录本次结案处理。':'约爬会被取消，参与者将收到通知。',success:async c=>{if(!c.confirm)return;try{await callCloud("calendar_plan_publish",{action:"resolve_report",reportId,resolution},{silent:true});await this.load();}catch(e){wx.showToast({title:e.message||"处理失败",icon:"none"});}}});}});}});
