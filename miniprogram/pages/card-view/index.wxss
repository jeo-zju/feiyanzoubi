/* 页面特有布局；元素样式统一由全局 app.wxss 提供 */

.card--canvas {
  position: relative;
}

.hero-menu {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  z-index: 2;
}

.canvas {
  border-radius: 18rpx;
  overflow: hidden;
  background: rgba(0, 0, 0, 0.35);
  border: 1rpx solid rgba(255, 255, 255, 0.10);
}
