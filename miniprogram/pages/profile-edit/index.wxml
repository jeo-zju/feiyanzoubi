<view class="page">
  <view class="card">
    <view class="card-bd">
      <view class="head-row">
        <view class="avatar-wrap">
          <image class="compact-avatar" mode="aspectFill" src="{{form.avatarUrl || defaultAvatar}}"></image>
          <button class="btn btn-ghost mini" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">更换</button>
        </view>
        <view class="head-right">
          <view class="inline-col">
            <view class="inline-label">昵称</view>
            <input class="input" type="nickname" placeholder="请输入昵称" placeholder-class="ph" value="{{form.nickName}}" bindinput="onNickNameInput" />
          </view>
          <view class="inline-col">
            <view class="inline-label">城市</view>
            <picker mode="selector" range="{{cities}}" value="{{cityIdx}}" bindchange="onCityChange">
              <view class="field-select">{{cities[cityIdx] || '选择常居城市'}}<text class="field-select__arrow">▾</text></view>
            </picker>
          </view>
        </view>
      </view>

      <view class="inline-row mt">
        <view class="inline-col">
          <view class="inline-label">名片名</view>
          <input class="input" placeholder="同昵称可留空" placeholder-class="ph" value="{{displayName}}" bindinput="onDisplayNameInput" />
        </view>
        <view class="inline-col">
          <view class="inline-label">头衔</view>
          <picker mode="selector" range="{{titleOptions}}" value="{{titleIdx}}" bindchange="onTitleChange">
            <view class="field-select">{{titleOptions[titleIdx] || '选择头衔'}}<text class="field-select__arrow">▾</text></view>
          </picker>
        </view>
        <view class="inline-col">
          <view class="inline-label">MBTI</view>
          <picker mode="selector" range="{{mbtiOptions}}" value="{{mbtiIdx}}" bindchange="onMbtiChange">
            <view class="field-select"><text class="field-select__value">{{mbtiOptions[mbtiIdx] || '未选择'}}</text><text class="field-select__arrow">▾</text></view>
          </picker>
        </view>
      </view>
    </view>
  </view>

  <view class="card mt">
    <view class="card-bd">
      <view class="inline-row">
        <view class="inline-col">
          <view class="inline-label">抱石</view>
          <picker mode="selector" range="{{boulderLevels}}" value="{{boulderIdx}}" bindchange="onBoulderChange">
            <view class="field-select">{{boulderLevels[boulderIdx] || '未选择'}}<text class="field-select__arrow">▾</text></view>
          </picker>
        </view>
        <view class="inline-col">
          <view class="inline-label">顶绳</view>
          <picker mode="selector" range="{{ropeLevels}}" value="{{topropeIdx}}" bindchange="onTopropeChange">
            <view class="field-select"><text class="field-select__value">{{ropeLevels[topropeIdx] || '未选择'}}</text><text class="field-select__arrow">▾</text></view>
          </picker>
        </view>
        <view class="inline-col">
          <view class="inline-label">先锋</view>
          <picker mode="selector" range="{{ropeLevels}}" value="{{leadIdx}}" bindchange="onLeadChange">
            <view class="field-select"><text class="field-select__value">{{ropeLevels[leadIdx] || '未选择'}}</text><text class="field-select__arrow">▾</text></view>
          </picker>
        </view>
      </view>
    </view>
  </view>

  <view class="card mt">
    <view class="card-bd">
      <view class="inline-row">
        <view class="inline-col">
          <view class="inline-label">身高（cm）</view>
          <input class="input" type="digit" placeholder="如 178" placeholder-class="ph" value="{{heightCm}}" bindinput="onHeightInput" maxlength="3" />
        </view>
        <view class="inline-col">
          <view class="inline-label">臂展（cm）</view>
          <input class="input" type="digit" placeholder="如 180" placeholder-class="ph" value="{{armspanCm}}" bindinput="onArmspanInput" maxlength="3" />
        </view>
      </view>
    </view>
  </view>

  <view class="card mt">
    <view class="card-bd">
      <view class="inline-row">
        <view class="inline-col">
          <view class="inline-label">名片一句话</view>
          <view class="slogan-row">
            <input class="input slogan-input" placeholder="写一句攀岩黑话（可留空）" placeholder-class="ph" value="{{slogan}}" bindinput="onSloganInput" maxlength="20" />
            <view class="icon-btn" bindtap="onRandomSlogan">🎲</view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class="card mt">
    <view class="card-bd">
      <view class="inline-row">
        <view class="inline-col">
          <view class="inline-label">💬 微信号 <text class="muted">（开启后展示在名片上）</text></view>
          <view class="contact-row">
            <input class="input" placeholder="选填，如 wx_rock123" placeholder-class="ph" value="{{wechatId}}" bindinput="onWechatInput" maxlength="30" />
            <view class="contact-switch">
              <text class="contact-switch__label">{{showWechat ? '展示' : '隐藏'}}</text>
              <switch checked="{{showWechat}}" bindchange="onToggleShowWechat" color="#8f7bff" />
            </view>
          </view>
        </view>
      </view>
      <view class="divider mt mb"></view>
      <view class="inline-row">
        <view class="inline-col">
          <view class="inline-label">🍠 小红书号 <text class="muted">（开启后展示在名片上）</text></view>
          <view class="contact-row">
            <input class="input" placeholder="选填，如 xhs_123456" placeholder-class="ph" value="{{xhsId}}" bindinput="onXhsInput" maxlength="30" />
            <view class="contact-switch">
              <text class="contact-switch__label">{{showXhs ? '展示' : '隐藏'}}</text>
              <switch class="switch-sm" checked="{{showXhs}}" bindchange="onToggleShowXhs" color="#8f7bff" />
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <button class="btn btn-primary save-btn mt" loading="{{saving}}" disabled="{{saving}}" bindtap="onSave">保存资料</button>
  <view style="height: 40rpx;"></view>
</view>
