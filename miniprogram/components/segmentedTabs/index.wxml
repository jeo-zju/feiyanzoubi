<view class="seg {{theme==='v2'?'seg--v2':''}} {{theme==='v2' && compact?'seg--compact':''}}">
  <view
    class="seg-item {{value===item.key?'seg-item--active':''}}"
    wx:for="{{tabs}}"
    wx:key="key"
    data-key="{{item.key}}"
    bindtap="onTap"
  >
    {{item.label}}
  </view>
</view>
