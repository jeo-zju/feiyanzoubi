/* 分页器：按钮使用全局 .btn.btn-ghost 体系，本文件只保留布局 */
.pg {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 24rpx;
}

.pg-btn {
  flex: 1;
  min-width: 0;
}

.pg-mid {
  width: 160rpx;
  text-align: center;
  font-size: 24rpx;
  color: rgba(231, 233, 243, 0.6);
}
