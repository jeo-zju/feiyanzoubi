<view wx:if="{{visible}}" class="sheet-mask" catchtap="onClose">
  <view class="sheet dcp-sheet" catchtap="noop">
    <view class="sheet__hd dcp-hd">
      <picker class="dcp-pick" mode="selector" range="{{years}}" value="{{yearIndex}}" bindchange="onPickYear">
        <view class="field-select">{{year}}年<text class="field-select__arrow">▾</text></view>
      </picker>
      <picker class="dcp-pick" mode="selector" range="{{months}}" value="{{monthIndex}}" bindchange="onPickMonth">
        <view class="field-select">{{month}}月<text class="field-select__arrow">▾</text></view>
      </picker>
      <view class="btn btn-ghost mini dcp-close" catchtap="onClose">关闭</view>
    </view>

    <view class="dcp-week">
      <view class="dcp-week-item muted">日</view>
      <view class="dcp-week-item muted">一</view>
      <view class="dcp-week-item muted">二</view>
      <view class="dcp-week-item muted">三</view>
      <view class="dcp-week-item muted">四</view>
      <view class="dcp-week-item muted">五</view>
      <view class="dcp-week-item muted">六</view>
    </view>

    <view class="dcp-grid">
      <view
        wx:for="{{cells}}"
        wx:key="key"
        class="dcp-cell {{item.empty?'dcp-cell--empty':''}} {{item.disabled?'dcp-cell--disabled':''}} {{item.selected?'dcp-cell--selected':''}}"
        data-ymd="{{item.ymd}}"
        catchtap="onTapDay"
      >
        <view class="dcp-day">{{item.day}}</view>
      </view>
    </view>
  </view>
</view>
