# 飞岩走壁｜用户端视觉重设计执行计划

日期：2026-09-09。状态：设计与交接方案，未开始实现。

2026-09-10 样板反馈更新：首页与“我的”已进入样板复修；V01、V05 及首页底栏以 `FRONTEND_SAMPLE_REFINEMENT_PLAN.md` 为最新执行规格。本文件其余页面规格继续有效。

2026-09-10 详情/发布更新：V02、V03 以 `DETAIL_PUBLISH_REFINEMENT_PLAN.md` 为最新执行规格，采用核心字段优先、选填收纳、详情信息去重的紧凑方案。

2026-09-10 剩余页面更新：除首页、我的、详情、发布外的20个用户/馆长页面，以 `REMAINING_PAGES_COMPACT_PLAN.md` 为最新执行规格；统一入口归属、控件与自然滚动适配。此更新不干扰正在执行的详情/发布任务。

## 1. 设计目标与判断

产品定位：通过一次具体的约爬认识新岩友。设计首先让人愿意赴约，其次让时间、地点、人物和报名状态一眼可读。

当前实现已经收敛了部分字号和控件，但 `.card`、`.social-panel`、描边输入框、圆角标签在各页重复堆叠，主操作紫色、Tab 金色和渐变同时竞争注意。首页接近筛选表格，详情接近数据面板，“我的”偏工具入口。统一元素并没有形成足够鲜明的页面构图。

本次依据 app.json 的 35 个注册路由、全局样式、主要页面 WXML 和入口关系制定；没有逐页真机截图，因此这里是新设计规格，不是已完成的视觉验收。旧 `FRONTEND_VISUAL_UNIFICATION_PLAN.md` 中“不做全面换肤”与本次用户要求不同，本文件作为后续用户端视觉执行依据。业务正确性继续以 `PRODUCT_ACCEPTANCE_REPAIR_PLAN.md` 为准，视觉任务不自行解决其中的报名、分页或迁移规则。

完成标准：既有统一的品牌感，又能明显区分“发现、赴约、认识一个人、记录攀爬、管理岩馆”五种任务。不能只替换颜色、字体和圆角就交付。

## 2. 唯一设计方向：岩场邀请函

整体气质：温暖、利落、有运动张力，像一张认真排版的周末攀岩邀请函。保留深色使用习惯，从蓝紫科技感转为暖黑岩壁、粉笔白和少量酸橙绿。

三种识别元素：

1. **时间签**：短日期配清晰的大时间，作为活动列表和详情的识别点。使用文字与细分隔线，像活动票签；不把每张卡做成复杂票券插画。
2. **岩友署名**：真实头像、昵称和常爬范围，组成邀请人的署名区。首页仅用已有发起人数据；没有成员头像接口时禁止编造头像堆、人数或“附近正在爬”的热度。
3. **路线笔触**：稀疏的折线路径与三个小圆点，仅用于详情封面角落、名片背景和空态。使用本地矢量源或轻量图片，面积小于所在区块的 15%，避开正文和触控区。

视觉约束：大块渐变、玻璃模糊、重阴影、发光描边退出通用界面；不使用全屏岩馆摄影背景；不把品牌英文或励志口号堆到每页。设计感来自文字尺度、信息节奏、留白和少量识别元素。图片缺失时版式仍必须成立。

普通列表不再层层嵌套卡片。整页最多一个重点面板，其余用开放排版、内容行和细分隔组织。圆角是容器细节，不是所有内容的外框。

## 3. 页面范围

以下名称均指 `miniprogram/pages/<名称>/index`，包括该页可触达的弹层、菜单、预览与结果态。

| 页面组 | 路由 | 覆盖数 |
| --- | --- | --- |
| 主流程 | home、calendar-mine、plan-detail、calendar-publish | 4 |
| 身份与关系 | me、profile-edit、friend-list、circle-list、circle-detail、circle-edit | 6 |
| 名片与岩墙 | card-view、card-gift、card-claim、wall | 4 |
| 日历与记录 | calendar-overview、calendar-timeline、checkin、activity-records | 4 |
| 触达与安全 | notifications、safety（普通用户部分） | 2 |
| 馆长业务 | owner、gym-manage、gym-merge、gym-merge-result | 4 |

共 24 页。馆长是实际业务用户，四个业务页面纳入，但密度偏实用，不套首页大标题构图。

排除 11 页：debug-logs、user-admin、review-queue，以及 backstage、backstage-auth、backstage-data、backstage-gym-auth、backstage-health、backstage-review-detail、backstage-sync、backstage-user-detail。safety 的管理员举报区保留功能，仅保证不被主题破坏，不作专门重设计。

不删除排除页、不改变路由和权限。名片保存图片、领取二维码、朋友圈/微信分享封面是用户可见输出，纳入相关页面验收；不可只美化页面外壳而遗漏生成图片。

## 4. 可实现的设计系统

### 4.1 色彩与语义

| Token | 值 | 使用规则 |
| --- | --- | --- |
| canvas | #151714 | 页面暖黑底色 |
| surface | #20241F | 少量重点容器、表单分组 |
| surface-raised | #2B3029 | 弹层、浮动操作区 |
| ink | #F3F1E7 | 标题、正文、关键时间 |
| ink-secondary | #B9BDB0 | 辅助描述、次级标签；不再用任意透明度叠色 |
| ink-muted | #969D90 | 非关键元数据，关键状态不用此色弱化 |
| line | #383E34 | 分组细线，不能作为唯一交互边界提示 |
| accent | #D2EF72 | 主按钮、当前选择、少量品牌符号 |
| on-accent | #1B2312 | 亮底按钮文字 |
| success | #B0D9A6 | 已确认/保存成功，配状态文字 |
| pending | #E8C58C | 待确认/处理中，配状态文字 |
| danger | #F1A19A | 取消/失败/删除，危险按钮独立布局 |

每屏亮绿实体区域以一个主操作为主；筛选选中用弱底色和文字，不再形成多个抢眼实心按钮。取消、满员、过期分开命名，不能全涂绿色或黄色。二维码继续用清晰白底深色码，保证静区，不染成主题色。

### 4.2 字体、尺寸与留白

- 使用系统字体，不下载中文字体；常规字重 400/500/600/700，避免全站 800/900。
- 54rpx/1.15：首页一句主标题与详情活动标题，最多两行；长标题按内容降至 44rpx 或自然增加高度，不硬截关键内容。
- 44rpx/1.2：普通页标题、重点时间数字；36rpx/1.3：区块标题；32rpx/1.4：列表活动标题；28rpx/1.55：正文/按钮/输入；24rpx/1.45：辅助标签。一般用户文字不小于 24rpx。
- 时间轴刻度可用 22rpx 作为唯一密集视图例外；具体时间详情仍以 28rpx 以上展示。Canvas 单独按导出像素比例定义字号，不能直接把 rpx 数值复制过去。
- 普通页左右 32rpx；320px 宽屏可降为 24rpx；区块间 40–48rpx，标题到内容 20–24rpx，同组行距 16rpx，细节间距 8rpx。
- 圆角：控件 16rpx、重点面板 24rpx、弹层顶角 32rpx；只给头像/状态点使用正圆，常规容器不全部药丸化。
- 主按钮高度至少 96rpx；其他可点击元素有效命中区域至少 88rpx。图标视觉尺寸可为 36–40rpx，透明热区补足；不要为了放大热区让相邻点击重叠。
- 表单字段以 96rpx 起步，标签在字段上方或左侧，不拿 placeholder 代替标签；普通列表行至少 112rpx，随多行文字增长。

### 4.3 导航与基础布局

保留原生导航栏与三个原生 Tab，降低适配成本。首页原生标题可简短显示品牌，正文承担内容标题；次级页原生栏显示页名，不再重复一条自制返回导航。Tab 图标采用统一 2px 线性笔触的本地资产，默认灰、选中酸橙绿。

普通页自然滚动。固定 CTA 仅用于发布、活动详情、资料保存、打卡提交和领取等必要场景。首页底部“发起约爬”置于原生 Tab 上方，实际位置按微信视窗验证；空态时正文不再放第二个同级实心发布按钮。没有固定 CTA 的页面不预留 200rpx 空白。

底部占位 = 按钮高度 + 容器上下内边距 + 设备底部安全区，仅一处负责安全区。键盘打开时表单焦点可见，固定按钮不得压住输入内容；优先使用页面滚动与原生输入调整能力。

### 4.4 共享组件合同

组件只处理呈现、用户输入和事件；不内置鉴权、云请求、缓存和报名业务判断。

| 组件/样式 | 规格与职责 |
| --- | --- |
| page-header | 标题、副文案、一个工具入口；首页可用较大标题，业务页紧凑 |
| plan-card | 时间签、活动标题、岩馆、发起人署名、最多两项标签、人数与状态；接收已计算状态，点击输出 planId |
| person-row | 72rpx 头像、姓名、第二行信息、尾部状态或操作；资料不足显示中性占位 |
| status-label | confirmed/pending/full/closed/cancelled/ended/error 的字形与颜色；业务传入文字及状态 |
| filter-chip / segmentedTabs | 标签带收缩文本与固定箭头；分段导航最多四项，小屏不挤压 |
| selection-sheet | 城市/岩馆长列表：标题、搜索、选中标记、滚动结果、空/错/加载；多选有固定确认栏，单选点选关闭 |
| form-field / choice-chip | 标签、帮助、输入、错误位置固定；类型和氛围均用同一多选样式，保持业务区别 |
| action-bar | 主按钮、可选一项次操作、loading/disabled/安全区；不处理提交逻辑 |
| page-state | 首屏骨架、空态、失败、无权限；列表尾部加载另用轻量行，不重复完整空态 |
| confirm / result | 原生确认保留用于破坏性操作；页内成功结果使用图形+事实+后续动作，不弹营销庆祝层 |

日期/时间继续原生 picker，触发器统一。城市/岩馆统一搜索弹层；不要把短枚举、日期和复杂搜索全部强行塞进一个弹层。列表图片懒加载，主题效果不用 Canvas 绘制文字；仅名片等已有生成输出使用 Canvas。

## 5. 各页面具体构图

### V01｜home：一张发给新岩友的邀请

从上到下：城市入口＋通知 → “找个人，\n一起爬。”主标题 → 近期/今天/明天/周末 → 岩馆、类型筛选 → 约爬列表。没有巨大宣传 Banner，常见手机首屏至少露出一张完整活动卡或完整关键信息。

卡片结构：上沿“周六 · 09.12 / 18:00–20:00”时间签；下方活动标题；岩馆；头像＋发起人名字和水平；底部“抱石 · 休闲爬”和“2/4 人 · 还可加入 2 人”。时间不是一行灰色细字，人物不再挤在卡片边角。整卡是详情入口，尾部仅箭头，去掉每卡重复的“查看并加入”按钮感。

卡片 28rpx 内边距、24rpx 卡间距；常规内容用 surface，不描粗边。满员/截止保持可读，仅状态区域弱化，不将整张活动透明化。onlyAvailable 文案由业务合同确定，禁止视觉 agent 自行在“有名额”和“可申请”之间改语义。日历收为筛选区次级入口。

空态：“这个时间还没有约爬 / 换个筛选，或发起第一场。”底部保留唯一发布主按钮。通知没有可靠未读计数时不用假红点。

### V02｜plan-detail：赴约信息优先

顶部以开放排版展示日期、活动标题、岩馆和时间；右上仅小面积路线笔触。下面顺序：状态说明 → 发起人署名与邀请说明 → 一起爬的岩友 → 集合与联络 → 分享/编辑等辅助操作。正文避免连续五个同型盒子。

时间/岩馆采用双行信息块；人数和加入方式并列但可换行。成员名单用 person-row，管理身份下待审批区独立，用“待确认 N 人”清楚标注；接受/拒绝按钮不挤成长昵称后的一小团。

未确认用户的联络区展示锁图标和“确认参加后可查看”；已确认但未填写时明确“发起人尚未填写”，不能伪装成权限不足。已报名状态突出“已确认参加”及集合信息，取消报名降为次级危险操作；可调整 UI 层按钮安排，但必须复用原 handler，状态分支不删。

底栏按后端状态显示报名、申请、等待确认、邀请岩友、再约一次等；取消/结束/移除/屏蔽/满员/截止都有文字。分享继续使用原生 open-type。任何新鲜感不能来自虚构的活动封面、位置地图或能力认证。

### V03｜calendar-publish：三段式轻表单

同一滚动页，不做强制多步向导。第一段“在哪、什么时候”：岩馆摘要行＋14 天日期条＋开始/结束时间。第二段“想和谁一起爬”：攀爬类型、氛围、人数、加入方式。第三段“补充一点”：短标题、说明；集合位置、联系方式和可见性按现有功能置于“更多设置”，已填写/非默认时显示摘要提醒。

段标题用 01/02/03 小号标记，靠 48rpx 留白分组，不额外套三层卡。人数输入旁固定说明“包含你自己”；保护能力是自述。日期选中使用浅绿底深字，今天加独立小点，禁用日期文本也可读。

底栏“发布约爬”或“保存修改”；错误出现在字段旁并能定位，提交错误仍保留现有行为。视觉 agent 不重新设计日期算法，R5 修复由功能包负责。原有数据绑定、请求幂等、编辑版本、预填字段均需保留。

### V04｜calendar-mine：下一次见面的日程

上方标题“我的约爬”，通知为工具入口。即将参加/我发起的/已结束继续保留。即将参加下第一场已确认活动采用重点时间面板：日期、时间、岩馆、状态和进入详情；其余为紧凑日程列表。待确认显式展示，不算下一场已约成。

只基于当前已取得的数据展示重点活动，不宣称是全历史最早；若第一页面没有已确认活动，不额外全量扫描。不要在列表中重复渲染同一重点活动。已取消独立标注，不能用“已结束”暗示已到场。攀爬记录作为底部次入口。

### V05｜me：有人的身份页

首屏为真实头像、昵称、城市、常爬范围和“编辑资料”。下方是一张可预览/展开的攀岩名片，压低其工具面板比重；分享、保存作为同一行次操作。我的岩友、岩友圈用两行宽松入口，不再用 Emoji 统计小卡。

后续按“我的攀爬”“账户与安全”两组文本列表组织通知、记录、屏蔽与安全等入口。馆长业务作为独立入口组，保持现有权限判断；管理工具入口存在但不提升到普通用户主视觉。版本号放页尾，不拿业务 ID 填界面。

### V06｜profile-edit：让对方先认识你

头像和昵称开场；基础信息、常爬水平、名片表达、联络信息四组。昵称是主信息，MBTI/头衔/身高/臂展为补充字段，不能与昵称同等强制感。等级选择统一 field-select，已选范围清楚展示。

“名片一句话”提供现有编辑/随机能力，以文字或统一图标呈现，移除随机 Emoji 按钮。联络开关必须表达真实可见范围；与公开投影存在差异时记录业务问题，不为了文案优美承诺“所有人可见”。保存操作固定，离开提示仅沿用已有功能。

### V07｜friend-list：联系的人，而不是数据表

搜索置顶，placeholder 明确支持的昵称/岩友号。用“我的岩友 / 收到的申请 / 发出的申请”清晰组织现有分组；默认内容以头像、名字、城市和常爬信息为主，rockId 降为第二行。

申请列表中“接受”为唯一强调动作，拒绝是文字操作；已发送以状态代替按钮。删除收进该行菜单或已有确认入口，不与“查看名片”并排高亮。空态引导回找搭子，避免继续指向旧时间轴作为主产品路径。

### V08｜circle-list：同城小群体

每个圈用圈标色小方块/首字、圈名、简介、城市和人数构成横向大行；用户自选圈色限于图标，不铺满背景。创建入口在页头，申请/待审批/已加入在尾部。名称两行，简介两行，其余截断进详情。无封面数据时不加假岩馆图片。

### V09｜circle-detail：圈子的客厅

圈名与描述作为头部，成员数和关联岩馆为次级信息。正文先成员，再关联岩馆摘要，圈管理收进右上设置。头像和姓名统一 person-row；待审批区只给有权限的人显示。已有成员弹层和馆列表使用同一 sheet。

非成员主按钮是申请加入，成员态明确“已加入”，退出/解散位于设置的危险区。本页现有 WXML 以成员管理为主，本轮不另造动态 Feed 或增加发帖模块。

### V10｜circle-edit：轻量圈资料编辑

圈标预览＋颜色选择置顶，圈名、城市、简介采用开放表单，关联岩馆使用多选 sheet；已选馆显示可移除的摘要行。创建/保存为明确文字按钮，替换只有“✓”的提交入口。保持圈色数据及多选值不变。

### V11｜card-view：名片作为作品展示

深色安静画廊背景，名片按真实比例完整展示，避免框套框；下方仅“保存图片”主按钮和现有更多操作。图片加载失败保留恢复入口，不能无限骨架。内容文案可保留产品幽默，但页标题使用“攀岩名片”，不强迫所有人使用“嘴硬”人设。

名片图内设计：粉笔白底、暖黑大昵称、一个主攀爬等级、辅助水平与个人一句话，品牌路线符号放角落；仅布局当前实际可导出的字段。已存历史名片原图保留；新生成模板另设版本，不批量改旧数据。页面预览/相册保存/分享输出必须同源，旧渲染比例和清晰度不能回退。

### V12｜card-gift：把名片送给一个具体的人

草稿预览 → 赠送方式 → 领取链接/码结果，形成一条竖向流程；淡化现有“1/2”管理表单感。消耗次数说明在生成动作前清晰展示，继续遵守已有消耗语义。生成后主按钮转换为转发，白底码区完整独立。

不要新增礼物动画或发送渠道；隐藏无用长技术 ID，必要复制功能以“复制领取信息”等现有可用行为呈现。区分草稿未完成、生成中、生成失败、成功可分享。

### V13｜card-claim：收到名片

标题“收到一张攀岩名片”，以预览图和发送者信息（仅接口已提供时）为重点，下方一段领取说明。主按钮采用与真实动作一致的“领取”/“领取并保存”；已领取、过期、无权限、失败要有独立结果态。禁止让按钮暗示领取即自动成为好友。

### V14｜wall：岩馆里的名片墙

馆名和名片数简洁置顶，正文固定比例两列名片格，320px 下若不可读则降单列；打开详情后看完整卡片内容。不要把整张图片中的小字当主要可读信息，格子外保留姓名。上墙为一个主操作，满容量的替换规则在执行前可读。长列表渐进加载图片，禁止堆复杂旋转卡片。

### V15｜calendar-overview：一眼看哪天有人爬

月/日期范围标题与城市、岩馆、圈筛选分层，避免一排三个完整输入框。日历为整页重点，日期数字清晰；热度改为同一绿色的有限深浅档，配“场次”或点标记，今天描圈、选中实心，不与热度混用。

选日期继续进入时间轴；圈列表置于日历下方且降为次级。发起约爬和今日打卡二者突出前者，刷新改为标准下拉或现有次级入口，不保留三个同等重量的浮动按钮。

### V16｜calendar-timeline：清楚的重叠时段

保留时间轴数据和布局算法。上方日期与岩馆，头像列头与时间刻度固定逻辑不变；底色中性，活动色带降低饱和度，选中活动用描边＋状态文字，不能只依赖颜色辨人。

短时段活动文字不足时只展示时间并允许点开，不能溢出相邻时段；详情弹层保留现有报名/退出/名片动作但层级简化。进入完整约爬详情可复用已知 planId 路由，新增该入口需检查旧数据兼容。横向滚动提示轻量且不遮挡。

### V17｜checkin：高效的攀爬记录器

馆名、当前周期和日期组成紧凑头部。类型切换下是等级行：大等级、累计/上限、加减步进器。行间细线取代卡片；加减目标保持 88rpx 热区，数量居中稳定，不随数字宽度跳动。

提交按钮“保存本次攀爬”，成功后显示实际已存摘要与“撤销本次”入口。按钮文字需与已有提交/撤销语义对应，不能把无周期记录或累计算法当视觉需求实现。无线路、达到上限、提交中、失败和撤销中有明确表现。

### V18｜activity-records：个人攀爬日志

标题“攀爬记录”，日期范围副标题；四格统计改成两列或一条主数据＋辅助数据，解决四列拥挤。现有指标口径未核实前沿用准确字段含义，不把打卡次数改称到场次数、不把计划数改称认识人数。

正文用日期分组日志行：馆名、攀爬类型/摘要、状态。现有服务端分页保留，页码控制紧凑；没有数据时给已有打卡或找搭子入口。若分组仅在本页，跨页同日期标题要按数据合并处理，不能制造重复记录。

### V19｜notifications：值得注意的消息

清爽消息列表，未读用细绿点＋标题字重，已读保持可读；每行标题、岩馆和可用的时间，整行跳详情。“全部已读”置于页头文字按钮。通知类型图标只在有可靠字段时映射，不根据自由文案猜业务状态。

无通知用小路线图形和“新的约爬消息会出现在这里”；读取失败明确重试。不得新增订阅推送承诺或伪造未读数量。

### V20｜safety：安静、明确、可信

顶部一段隐私说明，下方屏蔽名单和解除操作；解除继续确认或沿用现有行为。举报入口说明指向详情更多菜单。避免警告红铺满页面，普通说明中性，破坏性操作才使用 danger。管理员区延续原权限和功能，不重新设计工作台。

### V21｜owner：馆长工作入口

“我的岩馆”标题，馆数和抱石/难度线路统计以紧凑一行或两列展示；主体馆列表使用统一 gym 行，馆名/城市/管理入口清楚。新增岩馆使用“添加岩馆”文字按钮，不只显示加号。普通账号无权限/无岩馆两种状态分开。

### V22｜gym-manage：业务分区清楚

馆信息、当前周期、历史周期、线路录入按明显章节展示，长内容默认只展开当前任务所需部分。保存按钮靠近对应章节，不让一个底部按钮看起来能提交所有表单。线路等级编辑保持紧凑但足够触控。

删除、合并集中到页面末尾“危险操作”，与日常保存拉开 48rpx 距离，文字明确影响；审核等待状态在相关章节显示。不能删除现有确认或改变审核提交逻辑。

### V23｜gym-merge：可核对的迁移确认

源馆→目标馆的清晰比较头部，下方迁移摘要、周期冲突、映射表，最后是现有确认操作。小屏对照表纵向显示，馆名不能只截断到无法区分；冲突以文字和颜色双标。未满足条件时显示具体未完成项，不只灰掉按钮。

这页是馆长业务，不是调试页。仅改排版和呈现，不运行合并，不变更数据迁移逻辑，不替用户确认。

### V24｜gym-merge-result：事实明确的完成页

成功图标＋源馆/目标馆，随后迁移计数、周期映射和目标馆核对清单。“进入目标岩馆”为主操作，“返回我的岩馆”为次操作；继续合并降为普通文字，避免连续触发高影响行为。错误/部分结果只能按实际服务端结果展示，不能统一套“合并完成”。

## 6. 核心页面低保真排布

以下只定义阅读顺序，不要求执行 agent 按字符尺寸机械还原。

```text
找搭子                         约爬详情
杭州⌄                    通知  周六 · 09.12
找个人，                       下班一起抱石
一起爬。                       18:00—20:00 / 岩馆名称
近期  今天  明天  周末          [申请已提交，等待确认]
岩馆⌄  类型⌄  更多筛选          头像  林同学 · 发起人
────────────────────          一起爬，轻松交流……
周六 09.12      18:00—20:00     一起爬的岩友        2/4
下班一起抱石                   头像 姓名       已确认
岩馆名称                       ────────────────────
头像 林同学 · 常爬 V3           在哪里碰面
抱石 · 休闲爬   还可加入2人 ›   确认参加后可查看
────────────────────          分享约爬      更多
[         发起约爬         ]   [当前状态对应的主操作]
找搭子    我的约爬    我的
```

发布页用三段留白表单；我的约爬用重点下一场＋日期列表；我的用人物身份＋名片＋分组入口。四页不能仍是同一组卡片换文案。

## 7. 执行任务包与文件所有权

执行 agent 开始前记录当前提交、git status 和所负责文件差异。工作区有其他人的改动时基于最新文件增量改，不能把旧 worktree 整体覆盖回来。本文件不自动创建任务。

### D0：视觉样板与组件合同，先做

- 交付 token 表、组件状态样板，以及 home/plan-detail/calendar-publish/me 四页高保真样板与 320/375/430 宽对照。
- 样板使用明确标注的设计样例数据；样例不能进入生产列表或自动 seed。
- 四页体现本计划的构图差异后再批量迁移，避免 24 页改完才发现方向不对。若无法运行微信，标注“静态样板，待微信验收”，不能宣称真机通过。

### D1：基础主题与共享组件

- 唯一负责 `app.wxss`、`app.json`、`components/*` 相关主题、`images/icons` 和新增主题文件的执行者。
- 建议新增 `styles/user-theme.wxss`，所有新规则以 `.ui-v2` 及专有组件类限定。各用户页根容器加 `.ui-v2`；组件按实际样式隔离机制显式接入，不能假设祖先样式自动穿透。
- 用户页 JSON 单独设置导航色，并以对应页面 WXSS `page` 设置新背景；排除页维持旧导航/底色。不要直接改全局 `page` 或通用 `.card/.btn` 后让后台也意外换肤。
- TabBar 只服务三个用户页，可统一更新。发布主题变量前验证微信支持；允许构建期生成常量，保持唯一来源，避免为 token 引入新运行时依赖。
- 原有组件被后台共用时采用可选主题属性/新样式入口，默认保持原样。先保留旧样式，不能为“清理”破坏排除页。

### D2：约爬主流程

负责 home、calendar-mine、plan-detail、calendar-publish 的 WXML/WXSS/页面 JSON 和必要视图 JS。依赖 D1；与 `PRODUCT_ACCEPTANCE_REPAIR_PLAN.md` 中 R1–R6 的 JS 修改串行协调。只调整展示时优先保留 handler 名称和 payload，不能复制另一套业务状态机。

### D3：身份、社交与安全

负责 me、profile-edit、friend-list、circle-list、circle-detail、circle-edit、notifications、safety，共 8 页。依赖 D1；涉及 me 名片预览时与 D4 明确渲染接口，D3 不自行改 Canvas 模板。管理员权限逻辑原样保留。

### D4：名片与分享输出

负责 card-view、card-gift、card-claim、wall，以及实际被这些页面和 me 引用的名片渲染模块。先搜索真实调用链确定文件，不凭路径名称猜渲染入口。生成图片样式与页面样式一起交付；真实二维码与隐私字段不改。云端分享渲染若需调整，单列可审阅任务，不混进纯 WXSS 包。

### D5：日历和记录

负责 calendar-overview、calendar-timeline、checkin、activity-records，共 4 页。时间轴/热度图仅改视觉映射，保持坐标、分页、统计和提交逻辑；共用 dateCalendarPicker 的结构修改由 D1 统一合入。

### D6：馆长业务与全体验收

负责 owner、gym-manage、gym-merge、gym-merge-result；保持危险操作确认。完成后按下节执行覆盖检查，所有页面交付前后截图、已验证/未验证状态清单。发现功能缺陷记入问题清单，不以这次视觉范围为由顺手改业务。

调度顺序：D0 → D1 → D2 → D3/D4/D5（文件无交叉才可并行）→ D6。共享样式和组件始终单人负责；发现缺少基础组件时向 D1 提需求，禁止各页复制新版本。

## 8. 验收标准与限制

### 视觉验收

- 24 页各至少一张默认态截图。四个主流程页加 me/profile-edit 覆盖加载、空、错误、长内容与主要身份状态；有表单或弹层的页面补键盘、选中、提交中、禁用状态。
- 320/375/430 CSS px 宽度；iOS/Android 真机各一台抽查核心页、picker、开关、滚动、键盘与安全区。系统字体放大时关键按钮/状态可读。
- 活动时间、岩馆、人物、报名状态优先；长馆名 30 字、标题 40 字、长昵称和缺头像情况下无覆盖、无横向溢出。正文对比度目标至少 4.5:1，关键非文本控件 3:1，需实际工具测量，不把颜色表视作已达标证明。
- 不以海报数量衡量设计：卡片减少一层无意义框、形成清楚的阅读顺序，才算完成页面重设计。首页和详情仍能在缺图时有完整设计感。
- 图标笔触一致；多选有勾选标志；状态不只依赖颜色；原生 loading/disabled/share 能正常呈现。
- 名片预览、保存图、分享图分别验收长名字/多等级/空字段，二维码保持可扫码，隐藏字段不得因换模板泄露。

### 功能与性能护栏

- 路由、权限、所有已有主要入口和分享深链可用；页面不因修改按钮层级遗漏取消、退出、举报、审批或保存动作。
- 不增加首屏云请求，不引入远程字体/大图 Banner/连续动画/整页模糊；静态图形总新增压缩体积建议不超过 150KB（名片用户生成图另计），打包后报告实际增量。
- 反馈动画只用短暂 opacity/transform，建议 120–180ms；弱设备可完全关闭，不以动画延迟导航或提交。
- 微信 WXML/WXSS 编译、本地必要回归、固定设备请求数与耗时对照分别交付。业务功能测试沿用已有测试，不为每个颜色写无意义单元测试。
- 排除的 11 页至少做打开与公共组件兼容抽查，保证主题改动没有破坏功能，不要求重设计截图全覆盖。
- 编译通过不等于视觉通过，mock 通过不等于云端通过，静态样板不等于真机体验通过。任何未做项写明，不使用“全部完成”掩盖。

## 9. 给执行 agent 的任务模板

> 执行 FRONTEND_REDESIGN_EXECUTION_PLAN.md 的 D__ 包。以开始时最新工作区为基线，只修改本包页面与已约定的共享文件。采用“岩场邀请函”设计系统，按逐页构图重排，不仅换色。保留现有接口、路由、权限、表单语义和业务 handler；与功能修复包冲突时先协调文件所有权。交付变更文件、前后对照图、各状态实测结果、包体/请求影响和未解决项。不要推送、部署、修改真实数据或自动派发下一个任务。没有真机证据时明确标注待验收。

本次交付为该执行计划，未修改页面、未启动 agent、未触发部署。下一步建议先执行 D0，用四张核心页面样板确认设计质量，再展开全用户端实现。
