# 2026-09-10 晨间验收记录

## 结论

07:00 定时验收已启动，已使用 GPT-5.6-Luna 执行三个有边界的检查/修复任务，并由主任务复核。当前结论为 **待修复，不上传**：用户新增发现的“同一用户可组织/参加重叠约爬”仍存在，仅按最新要求编写了 `SCHEDULE_CONFLICT_ADJUSTMENT_PLAN.md`，没有实现这项新限制。

小程序本地版本已准备为 2.0.25，尚未上传；本轮云函数未部署。没有设置体验版、提审或正式发布。

## 本轮检查和修复

- 旧约爬参与记录：去除只读最近 200 条导致漏项的兼容限制；完整分页读取报名并按最新状态归并，终态不被旧有效报名复活。计划 ID 查询分批不超过 50，合并后保持分页排序。
- 发现页截止判断：处理缺失/null/0 的旧截止字段，避免旧记录被错误分组。
- 活动记录页：补全统计面板及打卡入口的缺失事件处理函数。
- 岩馆管理：修复切换期间草稿丢失风险，选择状态由 JS 提供，替换模板内不适用的 `indexOf` 表达式；长名称允许换行。
- 发布页：明确由应用后的设置值同步摘要，并补充折叠字段保留和取消编辑的回归。此项不以“setData 必然异步更新 this.data”作为问题依据。
- 用户可达页面：补齐 20 页导航栏主题设置，与已有 4 页保持一致；复核主题根节点与导航配置。

上述为现有外部 agent 改动基础上的补充修复，工作区同时包含此前全站页面设计工作，不能把全部差异归因于本次晨间任务。

## 验证证据

- `tests/regression.js`：32/32 通过。
- `tests/calendar-report-cancel.regression.js`：8/8 通过。
- `tests/publish-settings.regression.js`：通过。
- `tests/remaining-pages.regression.js`：通过。
- 72 个小程序 JS 文件语法检查通过；原生 WXML 编译检查 40 个模板通过。
- miniprogram-ci Summer 初轮完整编译通过，220 个输出；最终完整编译再次通过，220 个输出（2026-09-10 22:26 收尾复核），产物为临时目录中的 `compiled-final.zip`。
- 检查了非忽略源码的常见密钥模式，没有输出或备份配置密钥内容。

本轮没有真机、微信开发者工具实际页面截图或双账号云端并发验收证据。因此不能声称手机视觉、运行时交互和真实并发全部验收通过。

## 仍需处理

1. P1 时间重叠互斥：按独立计划实现并验收。服务端事务、退出/取消释放、旧数据接入必须一起完成。
2. 真机检查：小屏、长文本、底部安全区、表单键盘、滚动与固定操作区；覆盖实际用户页面与核心报名链路。
3. 旧报名兼容目前选择完整性：每次“我的约爬”分页仍可能读取调用者全部历史报名，存在读放大。完成审计迁移后应收敛至可信成员索引；本轮不声称性能已彻底优化。
4. R1–R6 及晨间修复仍需后续部署与云端回归；数据库迁移没有执行。

## 备份与交付

- 验收前源码快照：`.dbg/acceptance-20260910/before`，316 个文件；清单 `before-manifest.json`，SHA256 为 `603a4646b454b77f5ee1cd55deec5897f9b3dd2b739573682c11fa7ea52b88c1`。
- 验收后源码快照：`.dbg/acceptance-20260910/after`，清单 `after-manifest.json`。不包含依赖、配置密钥、Git 内部文件或备份目录自身。
- 本轮不提交/推送 Git，不上传开发版和云函数；保留当前工作区供新限制的执行 agent 接续。最新代码以工作区为准，快照用于回退和对比。
- 晨间一次性任务完成记录后删除，后续不会循环唤醒或自动上传。
