# 产品执行验收与修复建议计划

日期：2026-09-09。验收基线：主工作区提交 `6d778ec`（v2.0.24）；检查开始时工作区干净。

本次仅检查实际代码、运行本地隔离测试及编写本计划。未修改业务代码、派发执行任务、部署、迁移或调用真实用户写接口。以下任务均为建议，等待用户决定执行。

## 验收结论

继续完善现有约爬闭环，暂不扩展功能。`PRODUCT_EXECUTION_STATUS.md` 的“F1–F3 已全部完成”不能作为验收通过结论：F2 存在报名口径、预填校验和动态分页缺口，F3 有旧记录漏展示，且所述首页 30 秒缓存策略未出现在当前代码中。

| 范围 | 本次结论 | 证据与限制 |
| --- | --- | --- |
| F1 通知与取消 | 本地目标回归通过 | 8/8；真实 SDK 并发与通知权限验收仍未完成 |
| F2 预填与发现规则 | 部分实现，需要修订 | 预填字段及静态分段分页存在；前后端规则尚未统一，缺少预填边界测试 |
| F3 分页与返回加载 | 部分实现，不通过完整性验收 | 新成员索引分页存在；旧成员漏展示；首页仍每次重载 |
| F4 部署、迁移与真机 | 记录称已部署，本次未独立核实云端 | 未发现本轮专项迁移审计、可审核索引清单与真机实测交付；旧数据兼容影响不能留到最后处理 |
| F5 试用 | 未完成 | 无实际试用记录；可先准备人工反馈表和指标口径 |
| 视觉统一 | 本次不作通过判定 | 前端由另一 agent 修改；代码审查不能代替微信渲染、小屏及真机检查 |

## 已确认问题与建议任务

### R1 / P1：恢复旧成员和旧发起人的“我的约爬”可见性

位置：`cloudfunctions/calendar_plan_publish/community.js` 的 `mine_list`，以及 `index.js` 的 `update` 事务。

当前查询只接受 `participantIds` 包含本人，或非 v2 的本人发起计划。旧的非发起人成员即使有有效 `joined/confirmed/pending` 报名，也不会返回。代码注释明确把这部分可见性推迟到迁移完成，而状态记录显示迁移仍未完成。

另一个可复现问题：编辑没有 `participantIds` 的旧计划时，`recipientIds` 从报名受众取得，未补发起人；保存后被标为 v2。若无人报名，索引变成空数组，该活动随后从发起人的“即将参加”消失（“我发起的”仍能查到）。

建议：

- 将参与者索引定义为发起人加有效 pending/confirmed 成员，与通知受众分开处理；所有创建、编辑、报名和退出路径遵循同一规则。
- “本人发起”查询兜底不应依赖 schema 版本，避免索引异常时连自己的活动都不可见。
- 为旧成员提供有界兼容查询及合并分页，或在切换查询前完成可核验迁移；不能以隐藏旧活动换取读取性能。单纯恢复全量报名扫描也不作为最终方案。
- 迁移先做只读审计，列出缺失/空/不一致索引、重复报名、人数差异和无法判定身份的记录，再设计幂等补齐及回滚；本计划不执行线上迁移。

验收：旧 `joined/confirmed/pending` 成员可发现并进入活动；取消/移除成员不误回归；旧计划编辑后发起人仍可见；至少 45 条混合新旧记录、相同开始时间、三个标签跨页不重不漏。记录每页查询数与返回文档数。

### R2 / P1：统一“满员审批活动能否申请”合同

位置：`calendar_query/discover.js` 的 `joinableWhere`，`calendar_plan_publish/lifecycle.js` 的报名事务，`miniprogram/utils/plan.js` 和 `pages/plan-detail/index.js`。

实际服务端行为是：审批报名写入 pending，不增加已确认人数，只有 `nextCount > capacity` 才失败。因此已满但未超员的审批活动仍可申请。详情页允许“申请加入”与此一致；首页却把全部满员活动归为不可报名，并从 onlyAvailable 排除。discover 注释所说“满员审批同样拒绝 PLAN_FULL”不符合实现。

建议本轮采用简单规则：满员时暂停新申请；已有待确认申请保留，释放名额后再确认。这样与“仅看可报名”、当前首页提示及本轮不做候补的范围一致。这是待执行的产品选择，不是当前服务端行为。如决定保留满员申请，则必须同步首页筛选、排序与提示，并明确申请不占位、等待不代表约成。

验收：直接/审批 × 有名额/满员 × 截止前/后，各入口、筛选和写接口一致；已有报名的幂等重试仍正确；满员确认失败且不改变人数。

补充：旧数据缺失截止字段时，运行代码实际以结束时间兜底，多处注释却写成开场时间；发现查询又依赖 `isFull`，详情可按人数推算满员。需统一旧字段缺失、null、0 和人数标记不一致时的处理及审计策略，不能只改注释宣称合同已统一。

### R3 / P2：处理发现列表跨页状态变化

位置：`calendar_query/discover.js` 分段游标；`pages/home/index.js` 直接 concat。

静态数据的两段排序测试通过，但分段归属会随报名、取消及时间变化。复现：21 条可报名记录，第一页返回 p00–p19；将 p00 改为满员，第二页返回 p20、p00，出现重复。反向变化也可能让已越过游标的活动本轮漏掉。现有测试只覆盖静态数据。

建议先明确一致性合同：本轮可采用实时列表，每页显示去重，刷新后重新排序并补全状态变化；如果要求一次分页遍历严格不重不漏，需要设计稳定快照/版本机制，普通键集游标不能单独保证。客户端按计划 ID 合并是防重措施，不能宣称同时解决漏项。

验收：翻页期间名额增减、报名截止、开始时间修改；记录是否需要刷新才能出现变化。验证游标的段值与字段，覆盖可报名段恰好 20 条、关闭段为空、跨段多页和旧游标处理。

### R4 / P2：首页返回缓存实际尚未实现

位置：`miniprogram/pages/home/index.js:8`、`:15`、`:18`。

`onShow` 无条件 `load(true)`；reset 清空列表与游标。`_lastLoadAt` 仅被赋值，未用于过期判断；发布/报名写入的 `plansDirty` 未被首页读取或消费。故状态记录中的“30s 过期 + plansDirty 写后刷新”未落地。

建议保留同一用户、筛选条件及日期范围下的列表和游标；在首次打开、过期、相关写成功、身份或日期范围变化时刷新。刷新期间保留可读旧列表，并显式处理失败。仅在有效刷新成功后消费脏标记，避免并发写入被清除。保留现有请求版本保护。

验收：详情往返 30 秒内无写操作不重复请求，滚动位置合理；发布/报名/退出后刷新人数；切用户、跨午夜、筛选切换、弱网失败与旧响应返回都不串数据。

### R5 / P2：补齐发布预填和提交校验

位置：`pages/calendar-publish/index.js` 的 `onLoad/onPublish`，`utils/plan.js` 的 `suggestedTime`。

复现：以日期 `2020-01-01`、人数 `20` 打开发布页，选中日期仍是旧日期，14 天日期格没有选中项；提交会调用发布 API。服务端拒绝这些值，当前未证实非法数据可写入，但前端体验和计划要求未满足。预填允许 2–20，服务端仅允许 2–12；页面停留至时段过期后也没有提交前重新处理。

建议统一严格日期、HH:mm、未来 14 天、30 分钟至 12 小时、不跨日、整数人数 2–12 校验。非法/过期新建预填给出可见的有效建议；若提交时需调整日期时间，先更新并提示用户确认，不能静默改期后立即提交。编辑以原计划为准，不能当成再约重新选时间。预填未知类型不得把默认有效类型全部清掉，字段继续采用白名单，联系方式不复制。

验收：固定测试时钟，覆盖下午/深夜/跨月/跨年/周末、非法日期、第 14/15 天、页面跨午夜、30 分钟/12 小时边界、人数 1/2/12/13/20/小数、无类型首页、未知类型参数、有效编辑和私密字段不复制。本轮主工作区未见此前讨论的独立预填回归文件，需随最终实现提交。

### R6 / P2：只有发起人时不应锁定可见范围

位置：`calendar_plan_publish/index.js:380` 附近的 `recipientIds.length` 判断。

新计划的 `participantIds` 包含发起人，因此长度从创建时就是 1。当前将任何非空参与者数组当作“已有岩友报名”，导致无人报名的新活动从 public 改 friends 也返回 `VISIBILITY_LOCKED`。已用隔离接口复现。

建议先排除发起人，再按有效待确认/确认成员判断是否锁定，复用 R1 的身份规范；通知受众不要直接充当成员数量判据。

验收：仅发起人时可改范围；存在待确认或已确认成员时按约定锁定；全部退出后行为明确；并发报名与编辑不能使已加入成员突然失权。

## 测试证据

- `node tests/regression.js`：16/16 通过。
- `node tests/calendar-report-cancel.regression.js`：8/8 通过。
- 使用现有内存数据库 harness 做额外只读代码加载与隔离调用，未写真实数据库：满员审批返回 pending；旧成员 mine_list 返回空；旧计划编辑成功后发起人 upcoming 返回空；仅发起人修改范围报 VISIBILITY_LOCKED；跨页满员变化产生重复 ID。
- 页面 VM 探针：过期日期/20 人预填未本地拦截，调用了模拟 publish；两次首页 onShow 均 reset=true。

现有 16 项中，F2 新增的是发现排序、筛选、字段投影 3 项；F3 只有少量样本的成员查询 1 项，没有实际跨 20 条分页测试。不能将这些通过结果等同于 F2/F3 全部验收通过。所有事务测试均为 mock，不能证明真实云 SDK 冲突重试和索引行为。

## 后续执行顺序与交付要求（本次不启动）

1. R1 + R6：统一成员索引、编辑与旧记录兼容，优先保障已存在活动可访问。
2. R2：确认报名合同并统一首页、详情、服务端及注释；补关键状态回归。
3. R5：预填校验与时间边界，提交可复现的固定时钟测试。
4. R3 + R4：明确分页一致性与返回刷新策略，再验证请求次数及状态变化。
5. F4：交付只读旧数据审计/迁移预览、基于最终查询的索引候选与验证记录、备份恢复步骤、部署版本证据；再安排已获授权的云端及真机验收。
6. F5：核心问题通过后小范围试用。提前准备人工记录：实际到场、是否认识新人、取消原因、是否愿意再约及 14 天再次约爬；未知不算成功，时间结束不算到场。

F4 真机至少覆盖发现→申请→确认→联系方式→退出/取消、最后名额竞争、重复请求、取消/审批竞争、私密链接与拉黑退出、打卡失败重试。并发最后名额测试需至少两名竞争报名者及发起人。Android/iOS 各至少一台，记录冷/热首屏、详情和报名的请求数及耗时，另行检查长文本、字体放大、键盘、安全区和弱网。

部署成功、索引存在、迁移完成、真机通过分别记录，不相互替代。`debug_probe` 清理保持运维待办，不影响本次代码缺陷排序。订阅消息仍待账号能力与模板核验；如试用仅用站内通知，明确触达限制和阶段退出条件。

## 状态文件后续校正建议

将 `PRODUCT_EXECUTION_STATUS.md` 的 F2/F3 改为“部分完成，待本计划验收”，保留历史部署记录并标注来源。删除末尾“每 5 分钟自动派下一项”的过期工作方式，改为“用户决定执行后再启动修复；当前仅验收与计划”。原调整计划的部署状态也应与历史记录分开更新。

本次为保留其他 agent 的原始报告，未直接改写上述历史文件。以本文件作为当前验收结论；没有启动定时调度或任何修复任务。
