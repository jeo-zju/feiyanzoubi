# 产品实现复核与执行分工

日期：2026-09-09。依据 PRODUCT_ADJUSTMENT_PLAN.md 和当前工作区代码。本次只审查、运行现有测试和编写交接文档，不修改功能或样式。

## 决策

继续完善现有约爬闭环，完成 A—D 的退出条件后进入 E 小范围试用。暂不拓展即时聊天、内容广场、积分、AI 匹配或候补等功能。已有足够的功能供验证“通过约爬认识新人”，当前缺的是完整性、可靠性及真实使用证据。

## 阶段状态

| 阶段 | 当前证据 | 尚缺 |
| --- | --- | --- |
| A 正确性 | 权限、幂等、事务等首轮实现；本次 node tests/regression.js 12/12 通过 | 真实云 SDK 并发/故障验证；审批、通知、举报的回归覆盖；旧数据迁移审计 |
| B 主流程 | 首页、详情、发布、我的约爬已实现 | 双账号真实发现→报名→确认→集合信息→取消闭环；预填和排序补齐 |
| C 信任触达 | 持久化 RockPlanEvents 通知、屏蔽、举报、管理处理、再约一次已有代码 | 下架通知遗漏参与者；订阅消息未接入；边界场景验收 |
| D 性能视觉 | 发现列表游标分页、首页请求版本标识；视觉交给其他 agent | 我的约爬读取放大、返回首页重复加载、索引及真机性能实测 |
| E 试用 | 计划已定义指标 | 未见真实试用和指标采集验收证据 |

原产品计划顶部“尚未部署/集合未迁移”的描述已不完全反映历史操作：本会话曾部署部分核心云函数，并创建 8 个缺失集合、确认 19 个集合已存在（共 27 个）；不代表所有函数部署完成，也不代表字段迁移、索引和安全规则已完成。本次没有重新查询云端部署状态。小程序上传此前停在 WXSS 编译阶段，不能据此认定小程序上传成功或认证全链路已完成。

## 已确认缺口与任务包

### F1：通知与管理取消，最高优先级

文件：cloudfunctions/calendar_plan_publish/community.js、lifecycle.js；新增独立测试文件。

证据：resolve_report 的 cancel_plan 事件 audience 只包含发起人与举报人，没有参与者，违反活动取消需要通知报名用户的规则。

执行：与普通取消共用受众计算/事件语义；覆盖已确认与待确认用户，处理旧计划缺失 participantIds 的兼容；同一操作重试不重复产生事件。不得把联系方式放进通知公开字段。

验收：发起人、举报人、确认成员、待确认成员分别验证；取消后不能新报名或确认；重复处理仍幂等；无关用户不可读通知。真实云端测试另记，mock 不代替真实事务验证。

### F2：预填与发现规则

文件：miniprogram/pages/plan-detail/index.js、home/index.js、calendar-publish/index.js、utils/plan.js；cloudfunctions/calendar_query/discover.js。

证据：again 只携带 gymId/city；首页 publish 未携带攀爬类型；discover 仅按 date/startTime/_id 排序，未实现计划要求的“可加入优先”。onlyAvailable 只看 isFull，未对截止报名做等价判断。

先设计：明确“有名额”和“可报名”的区别、审批活动满员时是否允许申请；排序必须与游标一起调整，禁止只对单页排序。再约一次预填馆、类型、氛围、人数和报名方式，日期时间必须重新校验；联系方式不默认复制。

验收：结束/取消活动再约、今天已无有效时间、周末/跨月；筛选带入；同时间跨页不重不漏；截止、满员和审批的标识与服务端报名规则一致。

与样式 agent 协调：home/calendar-publish 的 JS 也可能被选择器重构修改，交接后串行实施，不能以“只改 JS”假设没有冲突。

### F3：分页与返回加载

文件：cloudfunctions/calendar_plan_publish/community.js、calendar_query/discover.js、miniprogram/pages/home/index.js；必要时已有 cache/session 工具。

证据：mine_list 每页先 rows 全量读取有效报名，再用计划 ID 集合查询；home.onShow 每次 load(true)，会清空列表重新请求。现有防旧响应覆盖值得保留。

执行：先记录读次数和数据量；设计可分页的用户活动索引/关系查询，不直接无限扩展 in 参数；先评估旧数据兼容。首页按用户与筛选条件保留结果，仅在过期或相关写成功后刷新。只读请求可合并，报名等写请求不得以通用缓存处理。

验收：多页历史和大量报名场景无缺失；返回详情后列表位置合理；写成功后人数更新；快速切换不串数据。索引根据实际查询确认，输出可审核配置，不能宣称已部署。

### F4：部署与数据验收

文件范围：deploy-tools、测试与迁移文档；不碰业务样式。

执行：梳理已改云函数及部署版本，查明 debug_probe 的 UpdateFailed；为上传失败设置明确停止/报告策略。先制作旧计划/报名字段与重复记录的只读审计报告、迁移预览、索引清单和恢复步骤，再执行已授权范围内的操作。创建集合不等于完成迁移。

验收：双账号及真实云 SDK 下最后一名额并发、重复报名、取消/审批竞争、打卡失败与重试、私密分享与拉黑退出全部走通。小程序编译上传与真机验收各自记录结果。

### F5：试用准备，以上完成后

最小到场反馈和认识新人反馈可先由小规模人工收集，避免为统计再开发一套大型系统。先观察实际约成、取消原因、认识新人比例和再次约爬；有真实需求再决定候补、收藏或轻量打卡。

订阅消息是优先评估的触达补强，先核验账号模板与授权条件；当前站内通知需可靠。若首批试用仅站内通知，应明确调整阶段 C 的退出条件，不将订阅能力标成完成。

## 模型分工与交接规范

建议基于当前宿主提供的模型选项，不作额度节省比例承诺：

- Astra：定义任务合同、状态/权限边界、方案选择、最终差异审查与关键验收。普通规划 medium；复杂并发/隐私验收 high。
- Sol：默认执行模型，medium；F1、F3 涉及事务/数据兼容时可 high。
- Terra 或 Luna：范围清楚的单点修复、文档和独立测试补齐，medium；复杂权限和事务不要单独交给轻量模型定方案。

先执行 F1，验收后再推进 F2/F3，F4 准备工作可独立进行。不要同时启动多个 agent 重复扫描全仓库。

每个执行任务只给：本文件对应任务包、目标文件、当前代码基线、禁止修改范围、验收案例。执行报告限制为变更摘要、文件列表、实际测试命令/结果、未解决项。Astra 仅检查改动及关联关键路径，无新增风险不重复全量审查。

当前大部分变更未提交。若新任务使用 worktree，默认分支可能缺少这些改动；应先形成可用基线，或明确从当前工作树包含未提交更改的状态启动。不要让执行任务从旧 main 实现一遍新功能。

可直接使用的调度指令：

> 创建一个新的功能执行任务，模型 GPT-5.6 Sol，推理强度 medium，基于当前工作树（含未提交改动）的隔离 worktree。执行 PRODUCT_IMPLEMENTATION_REVIEW.md 的 F1，只修改该包限定文件并补回归测试，不修改前端样式。完成后报告 diff、测试结果和未解决项，由本任务中的 Astra 验收。

本次只提供分工方案，没有创建执行任务或启动子 agent。
