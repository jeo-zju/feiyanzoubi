# 岩馆同步修复与一线城市初始化计划

日期：2026-09-10  
状态：已完成本地代码检查与局部逻辑复现，待实现；本次不创建定时任务、不调用地图采集、不写入云数据库。

## 1. 目标与范围

本轮“一线城市”按北京、上海、广州、深圳四城执行，不默认扩展到新一线城市。保留现有杭州等数据，禁止清库重建。

目标是收齐四城可发现、可核实的实体攀岩馆，形成可持续同步的基础名录，供发布约爬选择岩馆。包含独立岩馆和可对外使用的室内攀岩场所；自然岩场、商场入口、设备商、纯培训机构、已确认停业场所不直接作为营业岩馆发布。存在疑问的场所进入审核。

“全部初始化”不能等同于地图接口返回成功。单一地图源可能漏收或分类错误，因此验收分别报告采集范围完成度、正式入库数量、待审核数量和已知遗漏；存在未处理的检索截断或已知遗漏时，不标记“全部完成”。

沿用腾讯位置服务作为首个来源。不同品牌分店必须是不同岩馆，同一实体在多个关键词下命中只保留一个主馆。不得凭地图名字猜测线路、难度、馆长、周期、评分或营业时间。

## 2. 检查依据与现状

已检查：

- `cloudfunctions/rock_sync_gyms/index.js`：采集、相关性过滤、匹配、写入、审核入队及执行记录。
- `cloudfunctions/rock_gym_review_queue_manage/index.js`：审核与列表。
- `cloudfunctions/rock_gym_list/index.js`：用户侧可见范围与分页。
- `cloudfunctions/gym_owner_upsert/index.js`、`gym_owner_manage/index.js`：人工维护、合并/删除及来源关联。
- `miniprogram/pages/backstage-sync/`、`miniprogram/services/api/backstage.js`：手工触发入口。
- `deploy-tools/scripts/upload-functions.js`、`rock-gym-sync-design.md`、`debug-sync-write-timeout.md`。

现有链路有四个关键词、同次运行 POI 去重、来源留档、人工审核队列、删除/合并记录兼容，具备基础。主要问题集中在跨批次正确性、覆盖完整性和长任务可靠性。旧设计文档包含尚未落实的设想，不能视作现有能力。

本次没有读取线上岩馆数量、线上函数配置、地图 Key 或账户配额，没有证明线上已经发生某一具体误合并；下表区分代码缺陷和待实测风险。

## 3. 需要修复的问题

| 优先级 | 位置 / 发现 | 后果与处理 |
|---|---|---|
| P1 | `findExistingGym` 同城只取 100 条；只有同城结果为空才按名称回退，没有优先按来源 ID 直查 | 第 101 家以后及城市字段有差异的旧馆可能无法匹配，重复新增。改为来源映射直查、规范城市编码及有索引的窄候选查询，不能靠提高固定 limit 解决 |
| P1 | `scoreGymCandidate` 接受阈值为 1，标准化同名本身得 1，原始同名再加 0.6 | 同城远距离同名分店也可自动合并。增加地址/有效地理位置等独立证据；同名、多候选或接近分数一律审核；精确来源关联优先于模糊匹配 |
| P1 | 缺坐标归零，距离函数将两组 0,0 算作零米；精确地址相同再加 0.5 即可达到阈值 | 缺失坐标造成假近邻。缺失值保持 null，验证范围与坐标来源，有效坐标才参与评分；同商场不同场馆不能仅凭地址/距离合并 |
| P1 | `buildGymPatch` 不保护人工地址、电话、位置和城市；`gym_owner_upsert` 修改模式未同步记录人工来源 | 同步可覆盖馆长修正；人工模式清空也可能被重新推断填入。建立字段级来源/锁；旧已认领馆默认保护人工可编辑字段，差异进建议队列 |
| P1 | 新馆立即 `active + approved`，低模式置信度随后才进入审核；用户查询只排除 deleted/merged | “待审核”不等于尚未公开。区分实体可信度与模式可信度；实体存疑在候选层，只有可确认实体可入正式馆表；类型未知不虚构类型 |
| P1 | 定时调用仍依赖管理员 OPENID，入口固定 `triggerType: manual` | 普通无用户身份的定时调用被拒。新增可信服务端调度入口与权限隔离，不能凭 `event.Type === 'Timer'` 或传入管理员 ID 绕过认证 |
| P1 | 全关键词抓取结束后逐条串行写来源、查重、写馆、入审核；无任务游标、租约和幂等提交 | 超时后部分写入、重跑重复来源/审核，两个任务同时运行可能重复建馆。拆页/拆写入批次，持久化进度与事务幂等 |
| P1 | 模糊 active 匹配早于 deleted/merged 精确来源判断；查重和重定向异常被吞掉 | 可能绕开删除墓碑、误连其他馆，或数据库故障被当成不存在而新增。精确来源→删除/多跳重定向→模糊候选；读取失败必须失败或重试 |
| P1 | 审核入队异常吞掉；审核先改队列、再更新馆且吞掉错误 | 运行报成功却丢待办，或审核显示完成但馆没改。馆与审核状态原子更新/幂等提交，失败可重试并计入任务结果 |
| P2 | `cities` 仅使用第一个；最多每关键词 5 页，默认 1 页；不标记截断，不校验返回城市归属 | 不能一次遍历四城或证明覆盖完整。任务显式分城市、行政区、关键词、页；触及限制标 incomplete，不报全量成功 |
| P2 | 相关性要求运动健身类目，黑名单覆盖整段名称/类目，未通过记录只返回少量样本 | 名字特殊或分类不准的真馆会漏掉。保留全部候选与排除原因，弱证据进入审核；补充检索不能直接放宽正式入库 |
| P2 | `saveSourceRecord` 不写主馆关联或后续处理结果；来源、审核均随机 add，`batchId` 精度到秒 | 重跑累积重复，合并时按 gymId 找不到这些来源记录。来源有稳定身份、每次运行的观测记录有幂等键，处理后回填 gymId/决定/版本 |
| P2 | dry-run 只采集，不模拟匹配/正式补丁，且仍写运行记录；写入按钮重新抓取；`write` 用真值转换 | 预览不对应实际变更；字符串 `"false"` 也会开启写入。改为严格枚举 action，预览产出完整变更集，提交绑定该快照和版本 |
| P2 | 每次同步都改 updatedAt，列表默认按该字段排序；每个 POI 重复查询同城 | 无变化同步也搅乱用户列表并造成读写放大。分离 lastSeenAt 与业务修改时间，内容哈希未变不改业务字段；使用来源索引和批内缓存 |
| P2 | 审核列表只读最多 200 条再内存分页；结果只返前 60 条 | 四城导入后旧审核不可达，完整报告无法从响应恢复。数据库游标分页、服务端筛选和全量清单导出 |

### 本地最小复现结果

以 Node VM 载入现有函数、替换云 SDK，不发网络请求、不写业务文件：

1. 同城同名、地址不同且相距数十公里：匹配得分 **1.6**，超过当前自动匹配阈值 1。
2. 两组缺失坐标归零后：距离 **0 米**。
3. `claimedByOwner: true` 的馆：生成的补丁将人工地址与电话替换为地图值。
4. 没识别出模式的新馆：主馆 `reviewState=approved`，同时 `shouldQueueReview=true`。这证明两种审核语义混用，不表示所有模式未知馆都应隐藏。
5. 模拟同城查询返回前 100 个非匹配馆：只发一次候选查询即返回无匹配，没有继续分页或按来源查询。
6. 模拟没有 OPENID 的 Timer 入参：返回 **FORBIDDEN**。

仓库有 OPEN 状态的同步超时记录，但没有线上实测配置；不能把“3 秒”当作已核实的当前云端超时值。

## 4. 修复后的数据与匹配设计

### 4.1 来源和实体分离

- 复用 `RockGymSyncRuns` 记录任务，增加 action、状态、城市进度、游标、租约、预算、重试、部分失败及完整性信息。
- 新增或等效实现 `RockGymSourceLinks`：确定性 ID 为 provider + providerPoiId 的哈希，映射 gymId、删除墓碑、重定向和版本。禁止依赖截断到 20 个的 `sourceRefs` 作为唯一身份索引。
- 复用 `RockGymSourceRecords` 保存规范候选与每次观测，幂等键包括 runId + 来源 ID；同一轮多关键词命中合并 provenance，不重复新增候选。保存原始证据与过滤决定的方式受地图数据使用规则约束，设置留存策略。
- `RockGymReviewQueue` 按 来源 ID + 审核类型 + 内容哈希 去重，区分 `entity_review`、`duplicate_match_review`、`field_change_review`、现有 `gym_mode_review`。旧审核类型保持兼容。
- 新增字段不要破坏现有 RockGyms ID、约爬关联、打卡历史、周期、馆长权益和已有合并重定向。

### 4.2 匹配顺序与并发

1. 精确查来源映射；已删除不复活，已合并解析至最终目标（限制跳数、检查循环及目标状态）。目标缺失或来源多重映射进入异常队列，不创建替身馆。
2. 无来源映射时，按标准 cityCode + normalizedName/地址特征/有效地理范围召回候选；使用稳定排序及完整候选分批，查询异常不当空集。
3. 自动关联必须满足强证据组合、唯一候选且无分店冲突。相同电话、坐标或名称均不能单独证明同馆；门店后缀不能过度删除。
4. 高可信且无重复候选的新实体可自动创建；实体或归属不确定先审核。匹配置信度、实体置信度、模式置信度分别记录。
5. 事务内原子创建来源映射与主馆/更新记录，确定性来源 ID 保证同 POI 重复调用只写一次；对不同 POI 指向同实体，采用城市写入租约、事务内重检和疑似重复审核。手工建馆也需接入同一去重服务，不能只有同步入口防重。
6. 人工变更在事务中重新读取字段来源和版本，不能用采集开始时的旧快照覆盖后来的人工作业。

### 4.3 展示与人工优先

确认是岩馆但类型未知：可以作为约爬地点被搜到，模式留空，不编造“抱石/先锋”，仅在需要模式能力的流程中引导补全。实体本身存疑的候选不进入用户列表；不能仅新增 pending 状态而忘记修正所有读取入口。

`lastSeenAt` 表示源中出现；`lastVerifiedAt` 仅在满足明确验证规则或人工核实后更新。一次搜索消失不等于停业，首次收录也不应伪称人工核验。地图与人工字段差异进入审核，不自动闭馆、删馆或合并既有实体。

### 4.4 补充：实体识别不能只靠关键词（2026-09-10）

本节作为 G1/G4/G5 的必要验收要求，优先于“名称命中即可高可信”的任何旧规则。目标优先保证正式库准确性，同时把证据不足的候选保留下来补证，不能为了凑齐数量直接放行。

#### 当前规则的可复现盲点

用**构造样例**运行现有 `assessPoiRelevance`（不是线上实际门店）：

| 构造名称 / 类目 | 当前结果 | 问题 |
|---|---|---|
| 攀岩装备体验中心 / 运动健身:体育用品 | accepted，0.95 | 装备店可能被当成岩馆 |
| 攀岩俱乐部办公室 / 运动健身:其他 | accepted，0.95 | 机构办公地址可能被当成活动场所 |
| 儿童乐园攀岩项目 / 运动健身:其他 | accepted，0.95 | 临时/游乐设施缺少可约爬场馆证明 |
| 极限运动中心 / 运动健身:极限运动 | accepted，0.75 | 极限运动并不等于攀岩 |
| 向上攀岩馆（某酒店店） / 运动健身:攀岩馆 | rejected，0 | 酒店内对外营业的真实岩馆可能被误排除 |

现有 0.95/0.75 是规则写死的分值，未经标注数据校准，**不能解释为 95%/75% 的真实概率**。搜索使用了“攀岩”不是新的独立正证据；同一个 POI 被四个关键词找到也不是四份证明。

#### 推荐方法：候选召回 → 实体分型 → 证据核实 → 三路决策

1. **关键词只负责找候选。** 不因名称含“攀岩”就写入正式表；品牌名不含关键词也可由官方门店清单召回。
2. **判断到底是什么场所。** 输出 `climbing_venue`（实体岩馆）、`mixed_sports_venue`（综合场馆内有攀岩区）、`amusement_attraction`（游乐项目）、`training_or_club`（培训/社团）、`retail_or_office`（用品/办公室）、`outdoor_crag`（自然岩场）、`unknown`。类别不清就保留 unknown，不能强行二选一。
3. **核实是否适合作为约爬地点。** 核对固定场址、持续存在的攀爬设施、当前对外使用条件及门店身份。实体是攀岩场所不自动等于公众可预约；学校/会员专用场地需要核实准入条件。儿童专门岩馆不一刀切排除，核实是否适合当前用户群，保留年龄/准入限制，不能当作普通成人约爬地点直接展示。
4. **输出三种结果。** `auto_accept` 仅用于达到强证据门槛的场馆；`needs_review` 用于证据不足、相互矛盾或特殊准入；`reject` 用于明确不在本轮范围的实体。候选决定与攀岩模式判断分开，待审实体不能先 active 再补审核。

#### 证据门槛，而非任意加分

| 证据 | 用法 |
|---|---|
| 品牌官方门店页、场馆官方预约/票务信息、人工已核实记录 | 强证据，需明确指向具体分店及地址，说明攀爬设施与对外服务，保存来源、摘录与核实时间 |
| 地图的明确攀岩细分类目、地址与有效坐标 | 用于定位和交叉核对，不能单独证明营业或对外开放；“运动健身”“极限运动”为宽类目，不作为明确攀岩类目 |
| 名称、介绍、设施照片、用户反馈 | 辅助证据；通用攀岩图片可能是广告，照片不得脱离门店归属和日期当作硬证明；可获得且允许使用才采集 |
| 装备销售、办公、临时游乐、私人内部场地等信息 | 识别实体用途或准入冲突；按语义和字段判断，不用整个名称的一处“酒店/学校”直接否决 |
| 多来源同名、同电话或同坐标 | 只能增强身份核对；聚合站转载同一上游不算独立来源，连锁统一客服也不能证明同店 |

**首轮自动入库门槛：** 有可追溯的官方/人工核实证据，明确该具体地址提供持续攀爬服务并满足对外使用条件，同时与地图场址一致、无删除/重复/停业等矛盾。官方证据缺失时，即便名字和地图类目都命中，也先进入待审；可批量人工核实后入库。此门槛是产品建议，首轮不使用未经验证的数字阈值替代。

第三方 API 未返回详情/照片/营业状态，不代表这些信息不存在，也不能据此编造证据或自动否定。先记录缺失原因，只对缺证据候选做补查；不为了每条 POI 都增加付费详情调用。

#### AI 辅助的位置（可选，首轮不依赖）

可以让低成本模型从已取得的名称、类目、官方介绍中提取实体类型、设施、对外开放条件和冲突点，减少人工阅读量。要求输出结构化字段、原文证据片段及来源 ID；没有证据必须输出 unknown。

模型不凭名称自由推测、不凭记忆补门店资料、不自行决定正式写库。输出须经过结构校验和上述硬门槛；模型失败回退人工审核。外部介绍中的指令仅作数据，不能触发工具或改变规则。按来源内容哈希缓存，仅新候选和证据变化时调用，并单独设预算。

#### 精度验收与存量纠错

- 首轮四城自动拟通过项**全部人工复核后**才第一次批量写入，形成真实标注集；后续自动化沿用已核实门店身份，对新实体继续应用证据门槛。
- 标注至少覆盖装备店、俱乐部办公室、培训机构、儿童游乐、滑板/蹦床中心、酒店内真岩馆、校园场地、品牌名真岩馆、停业/搬迁、连锁分店。明确不确定类，不能把未知默认标作负样本。
- 报告正式入库精确率（复核为真馆的入库项 / 已复核入库项）、已知真馆清单召回率、待审率、证据缺失率及样本量。真实世界全部岩馆分母未知，不能宣称测出了全城真实召回率。
- 后续免逐条复核前，建议在与调规则样本分离的验证集上达到至少 99% 入库精确率，并报告误差范围；样本不足时继续人工复核，不能用很少样例的“100%”证明可靠。该数值是拟定验收目标，不是当前结果。
- 误收案例回流规则和回归集；审核拒绝保存来源 ID、原因和证据版本，后续同内容同步不得反复入队或复活。证据发生实质变化可以重新审核。
- 对存量正式馆先只读重评并生成待核实清单；有约爬/打卡关联的疑似非岩馆不得自动删除。人工确认后再纠正可见性/选择资格，保留历史引用。

## 5. 四城采集覆盖计划

| 顺序 | 城市 | 规范城市代码（任务配置） | 策略 |
|---|---|---|---|
| 1 | 北京市 | 110100 | 先全市试采，再逐行政区补齐与核对 |
| 2 | 上海市 | 310100 | 同上，处理直辖市城市/区级字段差异 |
| 3 | 广州市 | 440100 | 逐区补齐，检查跨市搜索返回 |
| 4 | 深圳市 | 440300 | 逐区补齐，避免邻近城市误归属 |

以上是本项目拟用的 cityCode，不直接假设它等于供应商接口全部行政码；执行时从官方行政区列表校验并保存映射。行政区清单由数据源生成、审核后固定版本，不能手写一份以后不更新。

- 主检索沿用攀岩、攀岩馆、抱石馆、攀岩训练馆；补查 climbing/bouldering、已知品牌与具体门店，补查词按试采遗漏证据调整。
- 分页走到数据源可访问末页；供应商结果上限或本地预算触顶时记为截断，按区补查，必要时在供应商支持且许可的范围内再细分区域。不要用刷关键词反复请求同一页冒充覆盖。
- 返回记录核对实际行政归属和有效位置，搜索范围不能代替归属验证；四城外结果隔离，不把来源查询城市强写成实际城市。
- 维护每个“城市/区域/关键词”的页数、返回量、来源总数（如有）、唯一 POI、排除量、遗漏风险及完成状态。报告所有候选，不能只保留前 60 条。
- 用岩馆官网/品牌官方门店列表等可核实清单交叉检查已知门店；发现遗漏进入定向补采/人工补录。四城各抽查不少于 10 家或全量（不足 10 家时），覆盖连锁分店、独立馆、地址变动和模式未知馆。抽样不等于证明现实世界无遗漏。
- 接口范围与配额上限以实际腾讯位置服务账号为准；本次未获取到地图官方检索页面内容，页容量/总结果上限、数据留存许可等需执行前核实，不能把代码中的 5 页上限当成平台上限。

## 6. 自动化触发方案

### 6.1 运行位置与权限

采用项目云端定时触发器 + 可恢复任务记录，运行不依赖开发电脑、Codex 在线或人工保持后台页面开启。本文件是自动化的实施计划，现在不创建任何真实触发器。

管理员在现有后台发起 `tier1-bootstrap-v1` 初始化，接口校验管理员后只创建任务并返回 runId。私有调度函数领取任务并执行；平台定时事件使用受控函数调用权限，处理函数不开放普通小程序客户端直接调用。不能把用户可构造的 Type/TriggerName/Message 当成认证凭据；落地时验证当前微信云开发环境可用的服务身份隔离机制。

Key 只在云函数环境配置 `TENCENT_MAP_KEY`，移除客户端传入 Key 的正式路径，避免打印带 Key 的 URL。调度消息只包含任务类型或 runId，不放凭据，不伪造管理员 OPENID。

### 6.2 推荐默认调度

| 阶段 | 触发与行为 | 结束条件 |
|---|---|---|
| 试采 | 修复部署后手动启动一次 preview 四城任务；云端每 5 分钟领取一小批工作 | 全部区域采集和变更预览完成，或预算/错误暂停 |
| 正式初始化 | 在后台提交已审阅预览快照；建议下一次北京时间 02:10 开始，具体日期在启用时确定 | 四城依次完成安全入库，疑似项仍停在审核 |
| 自动续跑 | 每 5 分钟处理可执行分片，任务有租约；无待办立即退出，不发地图请求 | 失败分片按 nextRetryAt 重试，超过上限暂停并告警 |
| 初始化结束 | 停用初始化触发器/任务，生成一份完成或待处理报告 | 不无限重复全量导入，不静默重建同名任务 |
| 后续维护（可选，默认不启用） | 验收后另行启用每周一北京时间 02:10 四城刷新，沿用相同幂等写入与人工保护 | 每周批次独立结束；停业、重定位等只进审核 |

调度时区显式按 Asia/Shanghai 设计；实施时核对控制台触发器时区与函数内 UTC 的差异，部署后用一次近期触发验证真实开始时间。不能仅设置函数 TZ 就假设触发时间正确。

腾讯云支持定时事件与自定义消息，云函数运行时默认 UTC；这些能力支持上述方案，但不表示本项目已经配置成功。参考：[定时触发器说明](https://cloud.tencent.cn/document/product/583/9708)、[函数概述与时区](https://cloud.tencent.com/document/product/583/19805)。

### 6.3 任务状态、预算与恢复

- 状态：queued → fetching → preview_ready → applying → completed；另有 waiting_review、retry_wait、paused、failed、cancelled。采集完成但审核未完只能标记“安全部分已入库，待审核 N 条”。
- 分片初始粒度为一个“城市/区域/关键词/页”；写入每批建议 10 条以内，根据真实数据库延时和函数超时调小。抓取分片先保存候选及游标，不将四城放在同一个云函数调用里完成。
- 租约含 owner、expiresAt 和 fencing version；领取、进度提交做事务比较。旧 worker 即使超时后继续运行，也不能覆盖新 worker 的进度或重复提交。
- 重试从最后提交的页/记录恢复；失败前已经提交的数据不重复新增。普通管理员重复启动同初始化版本返回已有任务，只有明确重建版本才新建。
- 地图请求初始并发 1，保留当前至少 350ms 间隔作为起点，实际速率须不超过账号限额；429、短暂服务错误采用指数退避加抖动（最多 3 次），鉴权/配置错误直接暂停。
- 上线前必须填写每分钟、每日、整轮请求预算，按账户剩余额度留余量；缺预算禁止正式运行，不假定免费无限额。预算触顶记 paused_budget，次日有预算才续跑，不标成功，也不自动购买额度。
- 只有开始正式批次、完成、失败/暂停或需要审核时产生运营通知；无进展不反复通知，不给小程序用户群发消息。

## 7. 预览、写入与回退

1. 做集合/索引/权限与凭据存在性预检，输出结果不输出 Key。现有部署脚本只上传函数，不能视为已配置超时、角色和定时器；补足独立的配置/校验步骤。
2. 创建和验证必要索引：来源身份、城市候选召回、任务状态+nextRetryAt、审核状态+稳定游标。先回填旧 sourceRefs 的映射；发现一来源多主馆先人工处理，不能随意挑一个。
3. 运行 preview：允许写任务与候选审计记录，但禁止写 RockGyms 和用户业务数据；界面说明“预览不会修改正式岩馆”。计算 wouldInsert/wouldUpdate/wouldReview/wouldSkip/unchanged 及字段差异。
4. 正式提交绑定 previewRunId、候选快照 hash、规则版本与主馆版本；版本变化重新匹配或进入冲突审核，不再次抓取一批不同结果后套用旧预览。
5. 写入前导出受影响馆、来源映射、审核项的备份清单与哈希。只备份必要集合，不导出登录凭据和无关个人资料。
6. 先完成北京小批次试写并验证幂等，再分片完成北京，接着上海、广州、深圳。已审阅批次内的安全项自动推进，疑似项不阻塞其他安全项，但阻止整轮宣称完全完成。
7. 每次写入留 before/after、来源快照、规则版本与写入版本。回退只针对该批变更，使用版本比较保护后来人工修改；已有约爬/打卡引用的新馆不能直接删除，转审核/隐藏或恢复字段。
8. 不使用 `test_seed_data` 构造正式岩馆；不删除“这次没有搜到”的旧馆；不在此计划顺带合并馆、批量取消约爬或创建虚构周期。

## 8. 实施任务与验收

| 任务 | 内容 | 交付 |
|---|---|---|
| G1 | 拆分 provider/normalize/match/write 模块，修匹配、人工保护、来源身份 | 可测试的纯规则和全部写入入口清单 |
| G2 | 事务幂等、审核一致性、墓碑/重定向、旧映射回填 | 数据兼容工具、dry-run 报告、索引清单 |
| G3 | 可恢复 worker、权限隔离、租约、预算/重试、触发器配置 | 部署与停用步骤、一次真实触发验证 |
| G4 | 四城覆盖分片、完整预览及后台进度/审核分页 | 全量候选与差异报告，关闭页面也能续跑 |
| G5 | 试采→试写→四城写入→审核→验收 | `GYM_TIER1_INITIALIZATION_REPORT.md`、回退清单 |

关键自动化测试：

- 同城超过 100 家的来源精确匹配；北京/北京市及直辖市字段归一化；查询出错不新增。
- 同名不同地址远距离、同商场相近坐标、缺坐标、坐标异常、相同客服号码均不误合并。
- 同来源跨关键词/跨运行/两 worker 并发只有一个主馆；不同 POI 的同实体走保守匹配与人工复核。
- 删除馆不复活，多跳合并正确，循环/断链显式报错；同步过程中人工更新不会被覆盖。
- 人工模式、地址、电话与位置保留；未知模式不猜测；实体存疑不对用户公开。
- preview 不改主馆，write 参数严格验证；快照变更需重算；部分失败与租约过期后重试不重复写。
- 审核落馆失败不显示审核完成；超过 200 条审核全部可分页访问；来源处理结果可追踪。
- 普通用户伪造 Timer 被拒；合法云端触发无需管理员登录；限流、配额不足与配置错误可区分。
- 四城任务全部遍历，触及分页/预算上限明确 incomplete；后台关闭/电脑关机不影响云端续跑。
- 重复完整运行第二次无新增重复馆、无重复待审项；未变化的馆不因 lastSeen 更新改变业务排序。

最终报告逐城列出：检索区域/关键词覆盖、原始与唯一候选数、过滤数、新增/更新/不变数、待审数、失败与截断数、已知门店核对、实际请求消耗、起止时间、任务 ID、规则版本和备份位置。真实触发、数据库写入及用户搜索/发布选馆的冒烟验证通过后，才能记录该城初始化完成。

## 9. 执行前需要落实的配置

- 云端地图 Key 是否存在、所属服务是否允许当前 API 使用；只核验，不让用户在聊天中贴 Key。
- 实际函数运行时、超时、内存、数据库事务限制与受控调用权限。
- 地图请求配额、数据存储/展示规则、可用分页及行政区查询能力。
- 正式初始化日期、预算和四城试采结果；默认范围为北上广深，后续周更默认关闭。

本次只完成计划和代码检查，没有执行 G1–G5，也没有承诺已收齐四城岩馆。
