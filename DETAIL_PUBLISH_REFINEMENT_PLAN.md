# 详情与发布页：紧凑版设计与执行规格

日期：2026-09-10。状态：方案交接，未修改业务或页面代码。

本文件接续首页与“我的”的样板修订，覆盖 FRONTEND_REDESIGN_EXECUTION_PLAN.md 中 V02/V03。保留既有暖黑、粉笔白、酸橙绿和时间签；不增加页面、云接口或发布步骤。依据当前两页 WXML 制定，未取得本轮两页真机截图，尺寸均为待实测目标。

## 1. 为什么现在不够简洁

发布页：三个章节都有编号、标题和副文案；14 天日期全部展开；类型、氛围、人数、审批、标题、说明同时呈现。即使许多项可不填，视觉上也像一份长问卷。

详情页：日期、标题、时间与岩馆分占多行；发起人在署名和成员列表出现两次；人数重复；空说明用通用口号补齐；成员逐行铺开；分享在正文与发起人底栏重复。固定底栏之外还有整排编辑/取消按钮。

调整原则：一次看清、按需展开。减少重复信息、常驻字段与段落，不以小字、小热区或把关键集合信息藏起来换取短页面。

## 2. 发布页：四组核心信息就能发起

默认结构（示意，时间和馆名来自实际选择）：

```text
发起约爬                     ← 原生导航标题

岩馆       嘻呐攀岩（滨江银泰店）  ›
日期       明天 · 9月11日 周五     ›
时间       18:00  —  20:00
───────────────────────────
类型       抱石  顶绳  先锋  自动锁
总人数     −       4       ＋
           包含你自己

补充说明（选填）                ›
集合与联络（选填）              ›

公开 · 直接加入                设置 ›

[          发布约爬          ]
```

“时间”仍有两个独立的原生时间 picker，不把开始/结束改成一个无法编辑的文本。每个字段都有明确标签；长馆名换两行，别在默认值就截断。

### 字段处理清单

| 字段 | 新布局 | 默认与约束 |
| --- | --- | --- |
| 岩馆 | 首行摘要，点击搜索弹层 | 首页带入就直接显示；未选时显示“选择岩馆”，提交前必选 |
| 日期 | 单行当前日期，点击原生日期 picker | 明确年月日、星期；沿用未来 14 天校验，不在正文展示两行日期格 |
| 开始/结束 | 同一行的两块时间选择，视觉上一个时间段 | 沿用现有有效未来时段建议、时长/跨日校验；不给过期默认值 |
| 类型 | 四种真实攀爬类型的紧凑多选 | 沿用首页/再约预填及现有默认；至少一种，不新增类型 |
| 总人数 | 2–12 的步进器，当前数字清楚显示 | 默认 4，包含发起人；预填和编辑值不重置；沿用服务端限制 |
| 补充说明 | 默认折叠行，点击展开输入框 | 空值完全可以发布；展开后自动聚焦，有内容时在折叠行显示一行摘要 |
| 集合位置/联系方式 | 一条独立折叠入口，两字段展开 | 保持可发现性，不藏进多层设置；填过则显示“已填写”而不是直接露出联络文本 |
| 可见性/报名方式 | 底部正文常驻简短摘要，点击设置弹层 | 初建默认公开、直接加入；非默认值必须清楚写出，不要求用户重复确认默认值 |
| 活动短标题 | 移入设置弹层，标记选填 | 空时沿用服务端已有默认标题，不生成新标题算法；编辑已有值完整保留 |
| 氛围 | 移入设置弹层的可选标签 | 不默认替用户选“欢迎新手”；有值显示一行已选摘要 |
| 求保护员 | 移入设置弹层的可选需求 | 不混在四种攀爬类型里；保持现有数据字段，不能单独满足类型必填校验 |

设置弹层只呈现上述低频项，不放集合信息进去再套第二层。打开时以当前表单值初始化草稿，取消不修改，完成才应用；关闭不会丢失已填写内容。弹层长内容可以滚动，键盘开启后按钮/输入可达。

删除 01/02/03 大章节和三段引导文案；只保留“包含你自己”、联络隐私提示和必要错误。活动氛围不是必填，不用专门章节解释“找到对的岩友”。

联络区展开时展示：“仅你和有权限的已确认成员可见。”不默认复制微信号或上次联络信息，不因样式调整改变服务端授权；屏蔽限制继续生效。

### 发布交互边界

- 不增加“下一步/预览/再次确认”的强制流程；核心信息正确即可一次提交。
- 表单行只是收纳，原数据仍保留。新建、编辑、再约分别沿用原逻辑；修改布局不能把折叠字段清空、把私密活动重置公开或把审批改成直接加入。
- 空标题使用当前服务端允许的默认行为，旧标题不得因为未打开设置而被覆盖。
- 岩馆选择允许取消返回表单，即使尚未选馆也不应强制困在弹层；发布时再提示缺馆。首次进入不自动弹多个 picker。
- 原日期/时间校验、幂等 requestId、版本校验、错误提示和提交中锁定保留。若当前 R5 修复已经生效必须复用，禁止为了换控件重写另一套算法。
- 校验失败若位于折叠项，自动打开对应区并定位错误；发生自动时间建议调整时仍要让用户看清，不能静默改期提交。
- 公开/审批等设置摘要在按钮上方的正文中显示一次；不要在固定按钮区塞成长篇说明。

## 3. 详情页：先判断是否适合，再看如何赴约

普通访客的默认结构：

```text
约爬详情                     ← 原生导航标题

周五 · 09.11                 18:00–20:00
下班一起抱石
嘻呐攀岩（滨江银泰店）
抱石 · 休闲爬                 ← 仅实际存在时

[头像] 猫老师 · 发起人              更多
       常爬 V2 · 5.8          ← 已有数据可推导时
想轻松爬一会儿，欢迎一起。     ← 有说明才显示

一起爬  2/4人   [头像][头像]      查看 ›
───────────────────────────
集合与联络      确认参加后可查看

[        报名一起爬         ]
```

这不是固定高度卡片；文字可以换行，内容缺失不渲染空块。顶部路线装饰移除，时间、岩馆和人物本身承担视觉重点。

### 信息去重与布局

- 日期和时间一行左右对齐，沿用首页时间签；标题降为 36–40rpx，馆名 28rpx。长标题最多两行的折叠展示需保留查看全文能力，不能丢掉实际内容。
- 发起人只保留一个完整资料行。成员摘要可以包含发起人头像以对应总人数，但名单不再在正文里重复其大资料块。
- 常爬信息只用接口已有字段；“能力由本人填写”作为资料查看内的辅助说明，不在每次约爬都占一行醒目位置。不暗示认证。
- 无活动说明时直接省略，不补“认识新岩友”等通用段落。有说明默认最多三行，“展开”看全文；操作要有充分热区，不能误触整块。
- 人数只在成员摘要显示一次；申请方式/截止规则可放一行辅助摘要，不重复强调三次。截止必须依据实际 joinDeadline 显示，“活动开始时截止”仅在数据确实如此时使用；旧数据不能硬写成同一规则。
- 分享/举报/屏蔽/编辑/取消改为单一“更多”弹层入口，依权限展示。分享在弹层使用真正 open-type="share" 按钮，不能假设 actionSheet 能直接触发微信分享。

### 名单与管理：摘要可见，按需打开

- 正文只放一条“人数＋最多三个真实头像＋查看”摘要。头像来自当前 detail 已返回的 ownerInfo/joiners，不新增查询，不生成虚构成员。
- 点击在底部弹层查看完整名单，保留发起人标识、既有移除、结束后加岩友功能及对应确认。长名单滚动，长昵称自然换行。
- 发起人有待审批时，在正文成员摘要下突出“待你确认 N 人 ›”，不能把待办藏进更多菜单；点击打开待确认名单及接受/拒绝。待确认不计入已确认人数。
- 申请数量大时不要让详情自然增高数屏。接受/拒绝后按原刷新逻辑更新结果及待办数，busy 期间禁用，错误保留可重试上下文。

### 集合信息：按身份变化，不一律折叠

- 未确认访客：一行“集合与联络 · 确认参加后可查看”，不用大空面板和多段提示。
- 已确认成员/发起人：集合位置和联络方式直接展开，放在发起人信息之后、成员摘要之前；这是赴约的关键内容，不应为了短页面再点两次。
- 可复制联系方式时保留文字按钮；没有填写显示短句“发起人尚未填写”。已屏蔽、失去权限与未填写不能混淆。
- 报名成功/待确认的状态说明只显示一次，辅助提示“结果与变更请查看「我的 → 约爬通知」”；不重复显示“尚未启用推送”等实现细节，也不承诺微信推送。

### 主操作合同

| 当前情况 | 重点内容与操作 |
| --- | --- |
| 可报名访客 | 底栏报名/申请，沿用现有业务状态判断 |
| 已确认成员 | 状态“已确认参加”及集合信息突出；取消报名作为次级操作，保留原确认流程 |
| 待确认申请者 | 状态“等待发起人确认”；取消申请是明确次级操作，不给人继续报名的错觉 |
| 发起人、活动有效 | 底栏“邀请岩友”分享；正文待确认入口清楚；编辑/取消放更多，不再有第二枚分享按钮 |
| 满员/截止/移除/屏蔽 | 对应文字状态与禁用报名，不能把列表折叠当作权限控制 |
| 已结束/已取消 | 底栏“再约一次”；名单内保留既有加岩友条件，不将结束视为已到场 |

已确认/待确认成员无需保留一条巨大的高亮“取消”底栏，可用紧凑状态条＋次级取消入口；取消始终可找到。状态行可自然高于 88rpx，不能因压缩导致长状态截断。优先保留现有状态函数，只调整展示位置和按钮视觉。

## 4. 两页共同尺寸

- 页侧边距 32rpx，320px 窄屏可为 24rpx。正文 28rpx、辅助 24rpx，不整体缩字。
- 分组间 24rpx，组内 12–16rpx，取消原有每组大标题与 40–48rpx 空隔。表单/可点击行 88–96rpx 起步。
- 日期时间同一行，长馆名有弹性；类型选择在窄屏自然换行，不强求四枚永远一行。
- 底部主按钮宽度 100%、高度 96rpx，与正文对齐。详情/发布是非 Tab 页，其安全区按本页实际视窗处理，不能照抄首页原生 Tab 的占位值。
- 默认发布页折叠状态目标：核心四组＋选填入口＋设置摘要约 700–850rpx；默认访客详情关键内容约 650–800rpx（不含原生导航及底部操作）。均为短内容目标，长文本/大字体允许增长。
- 不用固定总高度、不缩小点击热区，不为了“一屏”把已填写内容剪掉。

## 5. 执行包

### CP1：发布页收纳

文件：calendar-publish/index.wxml、index.wxss、index.js；必要的页面主题变体。实现单行日期 picker、人数步进器、选填折叠和设置弹层。复用已有数据字段与 handler；只给新展示交互增加适配函数。

### CP2：详情页去重

文件：plan-detail/index.wxml、index.wxss、index.js；必要的局部主题变体。合并日期时间、压缩署名、成员/审批弹层、联络区身份布局、更多操作和状态底栏。

### CP3：同设备验收

拿首页/我的相同设备参数验证四页一致性，交付详情访客/已确认/发起人待审批及发布默认/已填/编辑截图。共享 user-theme.wxss 只增加明确变体，不全局压缩所有 person-row、form-sec 或 sec 样式。

以上均为交接计划，本次不自动派发。执行前记录最新工作区状态，避免覆盖其他 agent 已完成的功能校验和样式；不改云函数、权限、接口 payload 或线上数据，不部署。

## 6. 验收重点

1. 新建无需填写标题、氛围、说明、联络信息即可发布有效约爬；已预填核心项时不要求重新选择。可选项保持可发现，不增加多步向导。
2. 编辑保存不丢任何折叠字段，尤其私密范围、审批模式、原标题、说明和联络；弹层取消不改值，错误可定位。
3. 2/12 人边界、必选攀爬类型、14 天、过期时间、再约与幂等校验继续有效；不回退既有修复。步进器不能让编辑操作绕过已确认人数限制。
4. 详情发起人/人数/分享不重复铺陈，成员与管理能力没有被删除。已确认用户无需展开才能看到有效集合信息。
5. 无权限、拉黑、未填写和加载失败是不同状态；折叠不是权限校验，不能把受限资料预先塞进页面再隐藏。
6. 320/375/430 宽、长馆名、长昵称、长说明、字体放大、键盘弹起、弹层滚动及底部安全区分别验收。必要现有回归继续运行，样式不另写机械测试。
7. 报告真实滚动高度和前后同数据截图，不只说“更紧凑”；微信实际渲染未验收时标注待验收，不能用静态样板替代。
