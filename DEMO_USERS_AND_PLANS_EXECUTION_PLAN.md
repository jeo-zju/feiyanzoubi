# 模拟岩友与近两周满员约爬执行计划

日期：2026-09-10  
状态：仅完成代码检查、图片文件清点和执行方案；未上传图片、未创建用户或约爬、未开启自动任务。

## 1. 目标与边界

使用 `C:\Users\jeo\Pictures\mockUser` 中的图片建立可复用的模拟用户池。管理员在后台点击一次，为所选城市补充未来两周的满员约爬，用于展示和体验页面在人较多时的效果。

- 真实用户可查看这些活动，但任何途径都不能加入、申请或与模拟身份建立社交关系。
- 昵称、攀爬偏好、人员组合及活动分布自然多样；按最新要求，用户页面不增加“演示/模拟”标签或说明，统一呈现普通满员活动。内部来源标记保留，不计入真实活跃统计。
- 活动全部满员，包含真实一致的模拟报名关系，不靠改一个数字制造满员。
- 沿用“上午／下午／晚上”时段模型，不恢复具体时间输入；主要安排在近期，最远为今天起第 13 天。
- 仅管理员按钮触发，不顺便增加每天自动造数据的定时任务。
- 不创建模拟打卡、评分、馆长、好友消息、付款、圈子帖子或线路数据。现有真实岩馆不被修改，不暗示岩馆官方举办了这些演示活动。

## 2. 已检查的项目现状

### 图片

目录下有 **67 张 JPG/PNG 图片，合计约 54.6 MiB，SHA-256 精确去重后仍为 67 张**。文件名大小写和格式混用，有较大的图片。本次仅清点文件及哈希，尚未检查每张内容、裁切适用性或视觉相似重复，不能将 67 张直接等同于 67 个独立人物。

### 现有代码

- `cloudfunctions/test_seed_data/index.js`：支持 seed/status/cleanup，已有环境开关与管理员检查，但会生成大量无关业务数据。约爬使用旧 dateKey/Date 对象等形态，缺少当前完整成员与满员合同，不适合直接作为本功能入口。
- 旧清理按名称后缀和 openid 正则匹配，不能用于新功能的精确批次清理；旧 seed 发现任何部分数据即跳过，不支持可靠续跑。
- `calendar_plan_publish/lifecycle.js` 的详情人数由报名记录重新计算（发起人 + confirmed/joined 成员），只设置 confirmedCount/isFull 不足以实现稳定满员。
- `calendar_query/discover.js` 将可报名局排在满员局前；`onlyAvailable` 会排除满员局。其白名单投影需统一返回满员与操作权限，用户界面不输出模拟来源标识。
- `miniprogram/pages/backstage/` 可增加一个管理员入口，沿用现有控件；不需要在首页或“我的”再加入口。
- 时间冲突模块已在其他工作中出现，执行前需核对 `calendar_plan_publish/schedule.js` 最终合同，不能为了生成成功绕开该约束，也不要覆盖其他 agent 正在修改的文件。

## 3. 后台交互

在现有后台工具增加唯一入口 **“演示数据”**，进入一页管理界面：

1. 城市：默认管理员当前城市，可更换；只列有已核实可用岩馆和可分配模拟用户的城市。
2. 密度：默认“适中”，可选“少量／适中／较多”，对应未来 14 天目标活动数建议 12／24／40。是目标总量，不是每次再追加这么多。
3. 主按钮：**“补充近两周约爬”**。点击立即创建幂等任务，返回状态与进度，页面关闭后任务仍可恢复执行。
4. 简短结果：新增 N 场、已有 N 场、跳过 N 场；资源不足则显示实际数量和原因，不能虚报成功。
5. 次级管理：查看最近批次、关闭演示展示、清理指定批次。清理先展示精确影响数量再确认。

首次素材准备属于部署流程，管理员日常点击按钮时不再读取电脑目录。云函数无法读取 `C:\Users\jeo\Pictures\mockUser`，不得把这个本地路径作为云端头像地址。

## 4. 图片与模拟用户池

### 4.1 素材准备

- 编写独立导入工具，全量读取目录中的 67 张图片，不做人工筛选，不因群像、清晰度或构图自动排除；不修改原图。只做可解码、格式和文件完整性校验，失败记录到后台并可重试，不能静默漏导。不按照片猜测姓名、年龄、能力等个人信息。
- 自动处理 EXIF 方向和头像尺寸，保持原图比例、沿用头像组件的 aspectFill 展示方式，导出适配的派生资源，去除定位等无关元数据；限制体积，不把 54.6 MiB 原图打包进小程序。
- 导出过程必须提供单独的裁切/压缩实现与验收方式，优先复用项目可用图片工具，不改变原文件。不增加头像人工审核或逐张确认步骤。
- 上传到独立云存储路径 `demo-assets/<assetVersion>/<hash>`，保存 cloud fileID，不保存短期临时 URL。页面复用已有临时链接解析，失败使用统一默认头像。
- manifest 记录每个源文件、哈希、资源 ID、版本、导入状态及对应模拟用户。重复执行复用资源；即使以后出现相同文件内容也保留文件到资料的映射，不做视觉筛选。

### 4.2 身份模型

- 首批按一张图片对应一个可复用资料建立 67 个模拟用户，全部图片都分配使用。文件技术性导入失败时任务标记待补齐，不把少于 67 个视作完整成功。相同资料的头像、昵称、能力在批次之间稳定，不每次重新抽取。
- 推荐复用 `RockUsers` 中的资料渲染能力，新增 `accountType: "demo"`、`datasetId`、`demoUserId` 和素材引用。ID 使用 `demo_` 独立命名空间，不能伪造真实微信 OPENID 或声称这些身份完成过微信登录。
- 若现有引用必须使用 openid 字段，可存内部合成标识，但所有认证均由可信微信上下文确定；登录、管理员授权、馆长权限、统计和社交接口必须识别并排除模拟身份。注册或资料更新接口不能让普通用户设置 accountType/datasetId。
- 资料字段与现有 `profile()`、userSnapshot 合同一致：nickName/displayName/avatarUrl、城市、攀岩偏好及能力等。模拟职业、联系方式、真实姓名、认证徽章均不生成。
- 昵称从人工编写词库组合，避免称“官方”“馆长”“教练”等容易误导的身份；简介最多一句，可留空。资料页不显示“演示用户”，通过统一的资料操作权限控制，不提供不可用的赠卡、加好友、联系等入口，也不能点击后假装建立了关系。
- 城市是稳定属性。同一模拟用户不能今天常驻北京、下一批又被随意分到深圳；新城市从未分配池领取资料，池不足就减少生成数量并报告，不用复制头像冒充更多不同人物。

## 5. 活动生成规则

### 5.1 日期与时段

按北京时间，以触发时实际时间生成今天至第 13 天（共 14 个日期），只选尚未开始的时段。跨午夜长任务在提交前重验，已过期的候选重排或跳过。

- 建议日期权重：未来 0–2 天约 60%，第 3–6 天约 30%，第 7–13 天约 10%。这是采样倾向，不强行每天凑数；今天没有未开始时段就分配后续日期。
- 工作日以晚上为主，周末偏下午/晚上，少量上午；具体权重配置化，不假设所有岩馆全天开放。若有可靠营业信息则校验，否则避免生成凌晨、闭店日或声称已核实营业时间。
- 只用 morning/afternoon/evening，默认单选，少量连续双时段；起止兼容字段由共用时段规则派生，不复制硬编码，也不向用户重新暴露具体钟点输入。
- 同一模拟用户组织和参加的时段不得重叠，跨批次也校验；多时段按真实所选集合占用，不把中间空档占满。默认每天最多一场，每周最多三场，避免异常活跃。
- 生成器用带 seed 的随机算法，同任务可复现；新一次补充使用新的任务 seed，但已有用户和活动不被随机重写。

### 5.2 岩馆与人员

- 从所选城市真实、有效、已核实可作为约爬地点的 RockGyms 中选择，不创建新的假岩馆。来源存疑、已删除/合并、已确认停业的场所不可用。
- 活动类型只取该馆已明确支持的模式；未知类型保留中性“攀岩”，不猜先锋或绳攀，不宣传保护资格或安全服务。
- 默认容量从 2–6 人中加权选择，以 3–4 人为主，**容量包含发起人**。成员全部来自模拟池，不抽取真实账号。
- 模拟用户偏好与活动类型相容，保留一部分稳定同行组合，也允许随机搭配；每次换发起人但不强求人人主持。选人失败有限次重试，不能无限循环或允许时间冲突。
- 同一馆同一时段默认最多 1 场演示局；岩馆间分布有轻微偏好但设上限，不把某馆刷成满屏。优先填近期空缺日期/时段，而非均匀铺满两周。
- 活动标题使用简短自然模板，如“下班一起抱石”“周末练练基本功”，不要生成“急缺一人”“求搭子”等与满员矛盾的招募文案。备注多数留空或一句，标签通常 0–2 个。
- 不编造刚刚有人报名、实时在线、已支付、真实到场或历史成绩。createdAt 使用实际写入时间，统计数据不伪装为业务活跃。

### 5.3 满员必须全链路一致

一个容量为 N 的活动必须同时具备：

- 1 名模拟发起人 + N−1 条不同模拟用户的有效 confirmed 报名。
- `capacity = confirmedCount = N`，`isFull = true`，participantIds 包含且仅包含 N 个模拟身份，joinSchemaVersion 使用当前版本。
- 所有成员快照、头像、时段占用、日期/城市、当前详情页返回人数与列表一致。报名 ID 使用现有确定性规则，重复提交不增加成员。
- 没有 pending，不填真实联系方式；时间保持当前规则，不能靠把截止时间设为过去来伪装满员。

单场的计划、报名和必要时段占用通过同一事务提交；批次可以分场续跑，但不能把只有主馆记录、没有完整成员的半成品暴露出去。优先先写为不可见的草稿/批次记录，通过校验后原子启用；启用门槛需覆盖所有读取入口。

## 6. 可看但不可参与

### 6.1 用户体验

- 普通列表、详情统一沿用已有“已满员”状态、字号和按钮样式，不增加演示标签、模拟说明、额外弹窗或特别文案。首次可见时就必须满员，不能先出现可加入按钮再异步禁用。
- 资料、成员列表和分享落地页同样不新增模拟标识；分享展示正常的满员活动，不生成“缺人/快来报名”等与状态矛盾的招募文案。
- 首页开启“仅看有名额”时应自然排除演示局，**不为制造热闹绕过用户筛选**；默认筛选沿用当前产品行为。后台注明这一显示条件，管理员可在普通列表验收。
- 真实可报名活动保持优先；演示局不强插在真实可加入活动前，不改变用户城市或日期条件。

### 6.2 服务端硬隔离

- 保留 `dataOrigin: "demo"`、`participationPolicy: "read_only_demo"` 等服务端内部字段，用于权限、统计和清理；公开投影返回通用的 availability/isJoinable/allowedActions，不靠用户页面识别并展示 isDemo。数据集、批次和合成身份字段不作界面文案。
- 正常用户路径由完整满员数据和不可点击的“已满员”操作阻止报名，不采用“先允许点击，再报演示禁止加入”的设计。join_plan、approve_joiner 等写入入口仍须在事务内独立拒绝真实成员写入，不能删除服务端保护。公开业务结果统一表示普通的 full/unavailable，不返回面向用户的 DEMO_READ_ONLY。
- 缓存过旧或异常请求仍到达服务端时：不写入成员、不返回“已参加”或假成功；客户端消费通用的不可报名结果并刷新活动为满员，不弹特殊错误、toast 或错误页。调整共用业务结果处理，避免被 cloud.js 的通用异常处理直接展示。网络或数据库故障仍按真实故障处理，不能伪装成成功。
- 兼容旧接口必须保留其能识别的普通满员合同，不能将拒绝包装为旧客户端理解的报名成功。验收覆盖旧版本；不能承诺任意不可控旧客户端绝无提示，必要时通过现有最低版本策略要求升级。
- 即使误改 capacity/isFull、清掉部分成员，或者旧客户端直接调 API，真实账号仍不能加入。生成器专用写入入口仅供管理员授权的服务端任务使用，不接受客户端任意 impersonate 用户。
- 模拟身份不能申请真实活动、赠卡、发送好友请求/私信、加入真实圈子、发布评论、占用真实用户日程或触发订阅消息。相关服务端入口建立统一 demo 身份保护。
- 数据库权限拒绝客户端直接写入角色、演示来源及成员状态；普通用户不可伪造 demo 标记来绕过其他约束。
- 统计/推荐/榜单排除模拟身份和活动；用户量、真实活动数、报名率等运营口径不包含演示数据。
- 对演示内容的真实举报可以照常受理以处理不合适素材，但不能生成模拟账号回复或影响真实用户权益；这类举报进入独立来源的审核记录。

## 7. 重复触发、任务与清理

- 建议新增 `demo_data_manage` 云函数及 `RockDemoRuns`、`RockDemoAssets`，后台通过 action=preview/generate/status/hide/cleanup 管理。名称以执行时架构约定为准。
- 管理员鉴权复用项目统一规则，另有服务端功能开关和允许环境配置，默认关闭；前端隐藏入口不替代后端鉴权。
- 每个任务记录 datasetId、runId、city、seed、生成器版本、素材版本、日期窗口、目标数、已存在数、成功/跳过/失败项、游标及租约。
- 默认“补充”模式：统计该城市未来 14 天有效演示局，低于目标才补差额。反复点击不会累加 24 场；同请求 idempotencyKey 返回同任务。
- 同城市任务使用可过期租约和提交版本防竞争；任务拆成有限批次。失败重试重用确定性 planId/joinId，不重复创建用户、头像或报名。
- 不在一个前端云调用内等待所有头像上传和全量生成。新 worker 的继续执行采用当前环境支持的受控服务端任务机制；此机制只续跑已点击创建的任务，不自动发起下一轮造数据。
- “关闭演示展示”通过服务端全局开关立即停止公开返回，刷新/缓存按数据版本失效。客户端直接打开旧链接也要得到隐藏/不可用状态，不能只从首页移除。
- “清理批次”先隐藏并停止相关生成任务，再按记录清单删除该批计划、模拟报名和该批占用。跨批共享的用户/素材保留，只有无引用且明确属于本数据集才能删除。
- 清理只使用 datasetId/runId + 精确资源清单及服务端校验，禁止沿用 `_v` 后缀或宽泛正则删除。出现真实成员/真实引用等异常则隔离报告，不自动删除关联真实数据。
- 到期活动自然不出现在未来列表；后台下次触发时可归档过期演示批次，不静默重排已生成活动日期。不要求管理员每天清库再生成。

## 8. 实施拆分

| 任务 | 内容 | 交付与依赖 |
|---|---|---|
| D1 | 67 张图片全量导入/manifest、上传资源、模拟身份合同 | 不设人工筛选，本地原图保持不变，云资源可追溯 |
| D2 | 统一 demo 隔离与公开投影、统计排除 | 所有真实交互被后端阻止；必须早于展示数据上线 |
| D3 | 可复现生成器、日期权重、选馆选人、完整满员事务 | 依赖最终时段与冲突模块，有限重试和跨批次互斥 |
| D4 | 管理后台单入口、进度、补差额、隐藏与精确清理 | 复用筛选框、字号、按钮，不重做其他后台页面 |
| D5 | 测试环境小批演练、完整性检查、体验展示验证 | 先 3 场，再常规密度；输出执行报告后按目标环境部署 |

建议新增纯生成器测试与隔离集成测试；不要直接修改旧 test_seed_data 来承担新功能。部署脚本默认排除了旧测试函数，新函数需明确列入目标部署清单，不能假设已部署。

## 9. 验收标准

1. 67 张图片全部导入并对应 67 个稳定资料，无人工筛选步骤；技术失败有明细和补导，资源 fileID 可解析、头像加载失败有回退，小程序包不包含原始大图。
2. 一键生成仅影响选定城市和未来 14 天，晚间/跨午夜/两周边界正确，时间控件仍为上午/下午/晚上。
3. 每场列表、详情、成员清单的人数一致且满员，所有成员为 demo，关系和日程约束完整。
4. 同模拟用户跨场/跨批次无时段重叠，无重复成员、重复报名或同馆同段无限堆局。
5. 真实账号在新旧客户端、直接云函数调用、容量被改大的异常情况下均无法加入；失败没有报名、占用、计数或通知副作用。
6. 模拟身份不能与真实用户建立好友/赠卡/圈子等关系，生成过程不发订阅消息或自动帖子，不改变真实统计。
7. 首页、详情、资料、成员和分享落地均不显示“演示/模拟”标识；页面初始呈现已满员且不可报名，不出现专属拦截错误或假成功。“仅有名额”不显示这些满员局，真实可报名局优先。
8. 重复点击、两管理员同时触发、执行到一半超时再续跑均不重复造数；资源不足有明确部分完成结果。
9. 同 seed 重放可复现规划结果，已存在数据在提交时重验；随机性不覆盖真实业务约束。
10. 关闭展示后旧链接与缓存不能继续公开显示；精确清理不影响真实用户、真实岩馆、其他批次或共享头像。
11. 回归现有发布、报名、详情、时段互斥、列表分页和管理员权限测试；在测试环境完成至少一个真实账号的端到端验证。

最终交付 `DEMO_DATA_EXECUTION_REPORT.md`：启用环境、素材版本/用户数、任务 seed 和 runId、逐城活动数与日期分布、满员一致性、隔离测试结果、资源清单、隐藏和清理步骤。报告必须明确数据为模拟，不作为真实活跃指标。
