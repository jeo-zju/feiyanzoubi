# 飞岩走壁：以认识新岩友为主的产品调整计划

日期：2026-09-08  
状态：阶段 A/B 已完成首轮代码实现；尚未部署线上、迁移数据或进行微信真机验收。

### 实施记录（2026-09-08）

- 新增找搭子首页、约爬详情、我的约爬、约爬通知、屏蔽与安全页面；旧日历和攀爬记录仍可从辅助入口进入。
- 约爬支持公开发现、人数上限、直接加入／发起人确认、取消／移除、版本校验、站内事件通知和幂等报名。
- 私密计划详情、名单和报名统一服务端鉴权；公开名片改为字段白名单视图。
- 打卡改为一次请求一个 submissionId，明细、日汇总和周期汇总在事务中写入，精确按批次撤销。
- 岩馆搜索改为服务端筛选分页；日历查询补齐城市和圈筛选；生产上传默认排除测试数据云函数。
- 新增 `tests/regression.js`：12 项隔离回归通过；104 个 JS 无语法错误；微信 WXML/WXSS 编译检查通过。

待完成：真实云开发 SDK／线上数据库回归、集合和索引迁移、微信真机视觉与弱网性能验收、订阅消息模板配置，以及小范围岩馆试用。

## 1. 目标与当前判断

已确认的定位：帮助攀岩爱好者通过一次具体的约爬，认识新的岩友。

核心流程：发现同城约爬 → 判断是否合适 → 报名并确认 → 按约一起爬 → 加为岩友／再次约爬。

第一版成功标准不是功能数量或名片生成量，而是用户是否通过产品和原本不认识的人一起爬，并愿意再次约爬。

当前已有约爬发布、报名、好友、圈动态、名片、打卡和管理功能。部分 README、PROJECT_NOTES 和测试用例仍把已实现功能列为待开发；实施以实际代码为准，先补齐正确性再调整体验。

已有检查：94 个 JS 文件语法检查通过，30 个注册页面及 Tab 图标存在性检查通过；私密计划越权、分享名片权限冲突、撤销跨提交误删、前端日期偏移已在本地隔离环境验证。未检查线上部署、数据库索引和安全规则，尚未完成微信真机视觉及性能验收。

## 2. 第一版范围

### 必须完成

- 首页从日历入口调整为同城约爬列表，支持先浏览再选择岩馆。
- 新增可分享的约爬详情页，统一报名、取消、成员名单和集合信息入口。
- 发布页简化为岩馆、时间、攀爬类型、活动氛围、名额和补充说明。
- 「我的日历」调整为「我的约爬」，即将开始的活动优先。
- 明确报名状态、人数口径、联系方式可见范围及变更通知方式。
- 修复权限、日期、报名并发、打卡一致性、筛选和搜索截断问题。
- 提供举报和拉黑入口，以及最小管理员处理流程。
- 统一高频页面视觉规范，减少重复请求并完成真机验证。

### 保留但暂不扩展

- 攀岩名片：用于判断是否适合一起爬，保留资料编辑和分享。
- 好友：作为见面后保持联系的方式，报名不以加好友为前提。
- 岩友圈：保留现有入口和动态，首页不再优先展示建圈／成员管理。
- 打卡：保留已有记录能力，修复可靠性；无周期的轻量记录列入后续阶段。
- 馆长与管理员功能：保持可用，后续按角色划分入口。

### 本轮不做

完整即时聊天、泛内容广场、排行榜、复杂积分、AI 匹配、商业化和全国铺开。

不迁移现有原生小程序和云开发架构。

## 3. 页面与信息架构

底部导航建议改为「找搭子／我的约爬／我的」。优先沿用现有 home、calendar-mine、me 路由，减少旧入口失效。

### 3.1 找搭子：首页

首屏结构：

1. 城市选择，记忆用户上次选择，不强制获取定位。
2. 日期快捷筛选：近期／今天／明天／周末。周末必须换算成明确日期区间。
3. 岩馆和攀爬类型筛选，默认全部岩馆；更细条件收进筛选面板。
4. 约爬卡片列表及「发起约爬」主按钮。

默认展示同城、公开、未取消、尚未结束的活动；可加入活动优先，其次按开始时间排序。满员活动明确标识，可选择仅看可加入。初版不做不透明的综合匹配分。

卡片展示：活动短标题、岩馆、日期与起止时间、发起人头像和常爬范围、攀爬类型、氛围标签、已确认人数与剩余名额。人数包含发起人，所有页面使用同一口径。

默认列表取代首页热度日历；日历／时间轴保留为辅助查看方式。浏览活动和打开公开详情无需先完成资料，报名与发布时才引导填写必要资料。

空态要区分：加载中、加载失败、同城无活动、筛选无结果。失败不能伪装成没有活动；无结果可清除筛选或带当前条件发起约爬。

### 3.2 约爬详情：新增 pages/plan-detail/index

- 展示发起人、计划信息、活动说明、人数和公开成员摘要。
- 展示活动状态与当前用户报名状态，不依靠按钮颜色表达。
- 主按钮随状态变化：报名／取消报名／等待确认／已满员／报名截止／活动已取消／活动已结束。
- 发起人可查看申请、接受或拒绝申请、移除成员、编辑和取消活动。
- 明确集合位置，以及由发起人自愿提供、仅已确认成员可见的联络方式。
- 支持计划分享深链；未知 ID、私密无权限、取消、删除等情况有独立落地状态。
- 名片以资料摘要展开，不强迫用户连续打开多层弹窗才能报名。

### 3.3 发起约爬：沿用 calendar-publish

必填：岩馆、日期、起止时间、至少一种攀爬类型。支持从首页筛选或「再约一次」预填。

建议默认：总人数 4 人（含发起人），可修改；报名方式默认直接加入，可切换为需要发起人确认。具体人数取值是本计划的实现建议，试用后可调整。

可选：短标题、欢迎新手／休闲爬／认真训练等氛围标签、常爬范围、补充说明、集合位置和联络方式。保护能力标注为本人自述，不显示成平台认证。

默认公开，好友／圈可见放入更多设置。取消当前默认「全天 10:00–22:00」，优先使用仍适用的上次时段，否则给出合理的未来时段并允许修改。

首版仍限定未来 14 天且不跨日。前后端同时校验真实日期、已过去的时间、时间长度和人数范围。提交中禁止重复操作，但仍必须有服务端幂等保护。

### 3.4 我的约爬：沿用 calendar-mine

- 第一屏展示下一场已确认活动，提供详情和取消入口。
- 列表按「即将参加／我发起的／已结束」组织；待确认申请显式标识。
- 历史活动采用真实服务端分页，不把有限数据下载后分页伪装成完整历史。
- 打卡记录和统计转为次级入口，避免首页展示大量含义不明确的计数。
- 结束后可「加为岩友」「再约一次」；时间经过只代表活动已结束，不自动认定实际到场。

### 3.5 我的与辅助页面

保留名片、编辑资料、岩友、岩友圈和个人记录。公开资料与仅本人可见资料分开返回；名片分享使用公开字段视图。

检查馆长入口：现有入口受管理员按钮显隐影响，需分别判断馆长和管理员角色，不能让真实馆长依赖管理员权限进入。

## 4. 报名、隐私和通知规则

### 4.1 状态与名额

- 计划状态：active／cancelled；结束状态由 endAt 判断，报名截止由 joinDeadline 判断。
- 报名状态：pending／confirmed／rejected／cancelled／removed。
- 直接加入：校验通过且有名额时进入 confirmed。
- 需确认：申请进入 pending，发起人接受时才占用名额；等待确认不等于约成。
- 已确认成员取消或被移除释放名额；满员检查、确认和人数更新必须原子完成。
- 同一用户对同一计划只有一个有效报名状态。重试不能生成重复记录。
- 发布者包含在总人数内且不能报名自己的活动。
- 修改人数不得小于当前已确认人数；首版有成员报名后不允许任意缩小可见范围，避免成员突然无法查看活动。
- 活动结束、取消或过报名截止时间后禁止新报名和确认申请。

### 4.2 可见性与联系方式

统一 canViewPlan、canJoinPlan、canManagePlan 和成员字段过滤逻辑。列表、详情、名单、分享、报名和通知不能各自采用不同权限规则。

公开计划可被发现；好友和圈计划必须校验当前关系／成员资格。不是通过 UI 隐藏实现权限。

报名不默认公开微信号、小红书号或其他联系信息。联络信息由提供者明确填写并选择用途，首版仅对发起人和已确认成员开放；公开接口使用字段白名单。

拉黑至少禁止新增好友请求、报名对方活动和相关发现推荐。既有共同活动保留必要的取消／退出通道，具体字段访问也需校验；不能只在列表隐藏。

举报记录最小字段：举报人、目标类型和 ID、原因、必要内容快照、处理状态和处理结果。后台支持处理和下架，处理动作留审计记录。

### 4.3 提醒

首版必须有持久化站内通知／未读状态，覆盖报名结果、取消、移除及时间地点变更。微信订阅消息作为补充，模板、账号能力和用户授权可用性在实施时核验。

拒绝订阅不阻止报名，不承诺用户一定收到微信推送；关键变化在详情和我的约爬中仍可查看。外发通知失败不应回滚成功报名，通过事件记录重试，并防止重复发送。

## 5. 数据与接口调整

### 5.1 沿用并扩展现有集合

| 集合 | 计划调整 |
|---|---|
| RockCalendarPlans | 增加 schemaVersion、title、cityKey、startAt、endAt、capacity、joinMode、joinDeadline、confirmedCount、atmosphereTags、version 等字段；时间戳用于比较，日期文本用于显示 |
| RockCalendarJoins | 统一用户与计划标识；增加状态、确认／取消时间；以稳定键或唯一约束保证一人一条；旧 joined 映射为 confirmed |
| RockUsers | 统一常爬范围和公开资料字段，保留旧字段兼容读取直至迁移结束 |
| 打卡相关三表 | 引入 submissionId／requestId；创建和撤销围绕一次明确提交进行 |

集合候选：RockNotifications（站内通知）、RockNotificationEvents（可靠发送事件）、RockUserBlocks（拉黑关系）、RockReports（举报）。正式字段和是否合并通知集合在接口设计阶段确定，避免不必要拆表。

### 5.2 接口边界

- 日历查询扩展约爬列表能力：服务端城市／日期／岩馆／类型过滤、明确排序和游标分页，只返回列表所需字段。
- 增加统一计划详情读取能力，返回可见字段、报名状态和允许的操作。
- calendar_plan_publish 扩展报名确认、编辑、取消和成员管理，统一鉴权和事务。
- 名片读取区分本人／持有者视图与公开分享视图，不能直接放开原始卡片文档。
- 保持现有 { ok, data, error, traceId } 协议；业务错误与网络错误分开处理。

先整理实际查询，再建立索引：城市＋状态＋开始时间、岩馆＋状态＋开始时间、用户＋报名状态＋活动时间、计划＋报名状态，以及身份／稳定键唯一性。字段顺序与可见性查询方案根据云数据库实际能力和执行结果确认。

### 5.3 迁移与旧链接

- 迁移前导出受影响集合并记录数量；先预览，再在测试环境执行幂等迁移。
- 旧计划补齐 cityKey、起止时间和状态字段；缺失或歧义数据单独列出，不静默推断。
- 旧计划没有人数上限时继续兼容，不自动设置为 4 人；由发起人下次编辑时确认。
- 旧报名可能重复，先去重审计，再建立唯一约束，回算 confirmedCount。
- 保持原计划／名片 ID 和旧入口可用；新详情页可通过旧 ID 解析。
- 多批次旧打卡无法可靠推断 submissionId，不继续用三分钟规则自动分组撤销；旧记录给出明确说明，新提交支持精确撤销。
- 灰度期保留兼容读取与功能开关；回滚前核对新数据兼容性，不能直接回退到仍存在权限漏洞的旧实现。

## 6. 实施阶段与交付物

| 阶段 | 工作 | 交付与退出条件 |
|---|---|---|
| A：发布前正确性 | 修复私密计划权限、名片分享、时区、城市／圈筛选、岩馆搜索截断、打卡批次与事务、报名幂等；生产排除测试函数并补权限 | 关键回归用例通过，数据一致性和越权测试通过 |
| B：核心找搭子流程 | 首页列表、计划详情、简化发布、我的约爬、名额和确认状态；同步设计最小可用通知 | 一条真实的发现 → 报名 → 确认 → 查看集合信息流程在测试环境走通 |
| C：信任与触达 | 站内通知、可用的订阅提醒、字段隐私、举报拉黑、管理员处理、再次约爬 | 拒绝订阅、拉黑、私密分享、活动变更和取消均有明确行为 |
| D：视觉与性能验收 | 统一高频页面、删除重复请求、完善缓存键和请求合并、查询分页及索引、真机测试 | 性能目标与视觉检查通过，更新说明文档和回归用例 |
| E：小范围试用 | 在同一城市少数岩馆组织真实约爬，观察加入率、到场反馈和再次约爬 | 根据实际反馈决定下一轮，不以虚假活动填充首页 |

依赖：状态、权限和字段合同先确定，再开发页面；通知事件与报名／变更事务一起设计，避免事后补触发器遗漏状态。公开上线前 A—D 必须完成，不只完成页面改版。

本计划不授权生产部署、线上迁移、真实用户推送或运营消息发送；这些操作在实现和测试结果可审阅后另行安排。

## 7. 视觉规范与性能策略

### 视觉

- 保留现有深色主风格，统一主按钮、选中态和状态色；金色强调不与紫色主操作争夺注意。
- 关键日期、时间、人数不再使用 17–20rpx 小字；正文与标签建立有限字号层级。
- 高频导航使用统一图标，减少不同设备表现差异明显的 Emoji 按钮。
- 一个页面一个主要操作，减少嵌套卡片、过多描边和连续多层弹窗。
- 正式版错误提示只说明用户可采取的动作，云函数名、栈和数据库部署建议放入诊断日志。
- 覆盖小屏、长昵称／长馆名、字体放大、键盘、底部安全区、加载和空态。

### 性能

- 移除首页已请求后再次预热的好友、名片和日历数据；只加载首屏实际需要的信息。
- 同 key 请求共享进行中的 Promise；缓存 key 包含用户、城市、岩馆、圈、日期范围和分页条件。
- 加入请求版本标识，快速切换筛选时旧响应不得覆盖新筛选结果；写成功后使相关缓存失效。
- 岩馆选择器不扫描全部历史打卡；最近访问偏好从轻量汇总读取。
- 列表服务端分页；统计采用聚合／汇总，避免固定 limit 导致漏数据及按日期／岩馆串行重复查询。
- 正常成功日志减少采样与存储次数，异步批量保存，联系信息脱敏。
- 先测主包和启动表现，再决定后台及低频页面分包，不把分包作为第一项优化。

建议验收目标（非当前实测结果）：固定设备、网络和数据规模下，缓存首屏可交互 ≤1 秒；冷启动首屏目标 ≤3 秒；常规列表查询和报名提交 P95 ≤2 秒。单独记录冷云函数调用，不掩盖其耗时；未达标时依据链路记录定位，不盲目增加缓存。

## 8. 验收清单

### 产品流程

- 新用户无需先选馆或填写完整资料即可浏览同城公开计划。
- 发布预填正确；下午、跨月、跨年及无可用时段时不选错日期。
- 报名详情显示真实状态、实际剩余名额和可用联络信息。
- 已确认成员能看到活动变更及取消；未授权订阅时仍可在站内获知。
- 结束活动支持加岩友和再约一次；不会自动认定已到场。

### 权限与数据

- 非好友／非圈成员通过已知 ID 调详情、名单、报名、分享均不可越权。
- 名片分享可打开，但私密字段不会出现在公开响应或日志中。
- 最后一个名额并发报名只成功一人；重复请求不重复占位；取消与确认并发不出现负数或超员。
- 难度和抱石同时提交，任一故障不会造成部分成功；重试不重复累计。
- 连续两次打卡，撤销第二次只影响第二次；撤销故障不造成三表不一致。
- 城市、岩馆、圈和日期筛选有效；第 201 家及之后的岩馆仍可按名称找到。
- 生产部署清单不含 test_seed_data；测试环境调用也有服务端权限限制。

### 性能与兼容

- 快速切换城市／岩馆无旧结果覆盖；返回页面不因重复预热再次全量请求。
- 同起始时间的活动跨页不重不漏；历史记录分页不是有限首批数据的本地分页。
- Android／iOS 各覆盖一台设备及弱网场景；记录首屏、详情、报名的请求次数和耗时分布。
- 真机检查长文本、按钮状态、低对比度、键盘遮挡及底部操作区。
- 旧分享链接、旧报名记录和旧打卡数据均有明确兼容行为。

## 9. 小范围试用与指标

事件最小集合：列表曝光、详情打开、开始报名、报名提交、报名确认、报名取消、活动取消、到场反馈、再次约爬、加为岩友。曝光按用户／计划／会话去重，不记录不必要的联系信息。

重点观察：

- 详情到报名转化、申请到确认转化及确认等待时间。
- 约爬实际发生率：通过参与者自愿反馈衡量，不能用「报名成功」代替。
- 认识新岩友的比例：区分之前认识、之前不认识及未知，不能只以是否已是应用好友判断。
- 首次约爬后 14 天内再次约爬的比例、取消与爽约反馈、举报处理时长。

阶段 E 先记录基线，再确定合理增长目标，不预设未经验证的转化率。

后续候选：无线路周期的轻量打卡、常去岩馆收藏、活动候补、更多圈内协作。只有核心约爬流程得到真实使用后再排期。

## 10. 下一步

按阶段 A 开始实施前，先将本计划的状态规则细化为接口字段和回归用例；随后完成首页、详情、发布、我的约爬四个页面的可审阅原型，再进入阶段 B 页面实现。
